# AI 聊天室国际化(zh/en)实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 AI 聊天室 v2 添加中英双语支持:前端 UI(~400 条)、后端系统消息(~60 key)、AI prompt(~45 段),运行时切换 + 持久化,prompt 语言跟随 UI 语言。

**Architecture:** 语言真源在后端(`data/settings.json`),经 `getLang` 构造注入流经 core;系统消息与 prompt 在发射时刻按当前语言烘焙(ChatMessage 契约零改动);协议标签(`<接棒>` 等)收口到根级零依赖叶子 `server/src/protocolKeywords.ts`,匹配用中英并集、prompt 教学按语言单侧;前端 vue-i18n@9,locale 镜像后端。

**Tech Stack:** vue-i18n@9(新增,唯一新依赖)、vitest(既有)、TS 函数式词典(后端零依赖 t())。

**Spec:** `docs/superpowers/specs/2026-09-08-i18n-design.md`(本计划从该 spec 展开;执行者须同时读 spec)

## Global Constraints

- **默认 zh 不变量**:settings.json 不存在、`getLang` 未注入、`lang` 参数缺省时,一切输出与现状**逐字节一致**。这是全部 15 个既有测试文件(1255 处中文断言)不修改仍通过的前提。
- **既有测试断言不动**:禁止为通过测试而修改既有断言;词典 zh 侧一律从源码**原样拷贝**。
- **分层契约**:core 严禁 import `'../store'` 与 `'./server'`;`server/src/protocolKeywords.ts` 零 import(同 paths.ts 范式);store 对 core 仅 type-only 引用;web 经 `@server` 别名仅 type-only + protocolKeywords 例外。
- **协议标签语义**:匹配 = 中英并集(不论当前语言);发射(prompt 教学)= 按语言单侧,标签字面量从 protocolKeywords 取,严禁 prompt 里手写标签。
- 每任务结束跑 `npm test`(server 包)与 `npm run typecheck`(根目录,两包),绿了才 commit。
- commit message 末尾带 Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>。

---

## Slice 1:协议地基(收口 + 双语化)

### Task 1: protocolKeywords.ts 根级叶子 + 接棒/沉默词表双语

**Files:**
- Create: `server/src/protocolKeywords.ts`
- Modify: `server/src/core/modes/baton/baton.ts`(全文重写为消费真源)
- Modify: `server/src/core/modes/subscribe/prompt.ts:121-140`(isSilentDecision 词表引真源)
- Test: `server/tests/protocol-keywords.test.ts`(新建)

**Interfaces:**
- Produces(后续任务消费,签名精确如此):
  - `export type ProtocolLang = 'zh' | 'en'`
  - `export const BATON_TAGS: { zh: '<接棒>'; zhLegacy: '【接棒】'; en: '<pass>' }`
  - `export function batonTagFor(lang: ProtocolLang): string`
  - `export const BATON_LINE: RegExp`(并集匹配,含 `<PASS>` 大写)
  - `export const BATON_STRIP: RegExp`(g 标志,剥尾行)
  - `export const BATON_END_WORDS: RegExp`(i 标志,英文词带 `\b`)
  - `export const AT_NAME: RegExp`(单捕获,`/@([^\s@,，。]+)/`)
  - `export const AT_NAME_GLOBAL: RegExp`(g 标志版本)
  - `export const USER_NAME_ALIASES: string[]`(`['用户', 'user']`)
  - `export const SILENT_EXACT: readonly string[]`、`export const SILENT_PREFIX: readonly string[]`
  - `export function isSilentText(raw: string | undefined): boolean`

- [ ] **Step 1: 写失败测试**

```ts
// server/tests/protocol-keywords.test.ts
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import path from 'node:path';
import { parseBaton, stripBatonLine } from '../src/core/modes/baton/baton';
import {
  BATON_LINE, BATON_END_WORDS, batonTagFor, USER_NAME_ALIASES, isSilentText,
} from '../src/protocolKeywords';

const members = [
  { id: 'm1', name: '甲' },
  { id: 'm2', name: '乙' },
  { id: 'm3', name: 'Bob' },
];

describe('协议标签双语并集解析', () => {
  it('<pass>@Bob 与 <接棒>@乙 等价解析到成员', () => {
    expect(parseBaton('第一棒\n<接棒>@乙', members, 'm1')?.nextMemberId).toBe('m2');
    expect(parseBaton('first\n<pass>@Bob', members, 'm1')?.nextMemberId).toBe('m3');
    expect(parseBaton('first\n<PASS>@Bob', members, 'm1')?.nextMemberId).toBe('m3');
  });
  it('<pass>end 触发结束;词边界防误触(recommend 不触发)', () => {
    expect(parseBaton('done\n<pass>end', members, 'm1')?.endDiscussion).toBe(true);
    expect(parseBaton('done\n<pass>end the debate', members, 'm1')?.endDiscussion).toBe(true);
    expect(parseBaton('done\n<pass>recommend Sarah', members, 'm1')?.endDiscussion).toBeFalsy();
  });
  it('<pass>user 交还用户(与 <接棒>用户 同义)', () => {
    expect(parseBaton('over to you\n<pass>user', members, 'm1')?.toUser).toBe(true);
  });
  it('stripBatonLine 双语剥除', () => {
    expect(stripBatonLine('正文\n<接棒>@乙')).toBe('正文');
    expect(stripBatonLine('body\n<pass>@Bob')).toBe('body');
  });
  it('BATON_LINE 不误配正文普通尖括号', () => {
    expect('see <code>blocks</code> often'.match(BATON_LINE)).toBeNull();
  });
});

describe('batonTagFor 按语言发射', () => {
  it('zh → <接棒>,en → <pass>', () => {
    expect(batonTagFor('zh')).toBe('<接棒>');
    expect(batonTagFor('en')).toBe('<pass>');
  });
});

describe('沉默/跳过双语判定', () => {
  it.each(['<跳过>', '【跳过】', '跳过', '<skip>', '<silent>', 'silent', 'Skip'])(
    '%s 判为沉默', (s) => expect(isSilentText(s)).toBe(true),
  );
  it('正文发言不误判', () => {
    expect(isSilentText('我认为这个方案有问题,理由如下…')).toBe(false);
    expect(isSilentText('I disagree with this approach.')).toBe(false);
  });
  it('空与 undefined 判为沉默(现状语义)', () => {
    expect(isSilentText('')).toBe(true);
    expect(isSilentText(undefined)).toBe(true);
  });
});

describe('架构防线:协议正则不内联(单一真源 tripwire)', () => {
  const SRC = path.resolve(__dirname, '../src');
  const consumers = [
    'core/modes/baton/baton.ts',
    'core/modes/subscribe/audience.ts',
    'core/orchestrator.ts',
    'store/trace.ts',
  ];
  it.each(consumers)('%s 引用 protocolKeywords 真源', (rel) => {
    const src = readFileSync(path.join(SRC, rel), 'utf8');
    expect(src, `${rel} 必须从 protocolKeywords 引入常量`).toContain('protocolKeywords');
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd server && npx vitest run tests/protocol-keywords.test.ts`
Expected: FAIL(`Cannot find module '../src/protocolKeywords'`)

- [ ] **Step 3: 写 protocolKeywords.ts**

```ts
// 协议关键字真源(全工程唯一):所有被"解析回来"的控制标签、词表与正则。
// 分层纪律:根级零依赖纯常量叶子(同 paths.ts 范式)——core/store/web 三方皆可引,
// 严禁 import 任何其他模块(含 core)。
//
// 双语语义:
//  - 匹配 = 并集(不论当前语言,中英标签都能解析:历史中文消息/英文模式输出/
//    AI 不听话混写,全兼容)
//  - 发射(prompt 教学)= 按语言选一侧:batonTagFor(lang) 等取单侧字面量
// 调用方:core(baton/audience/orchestrator/prompt)、store/trace(统计分类)、
//        web(MessageBubble/exportMarkdown 意图徽章,经 @server 别名 runtime import)。
// ⚠️ web/vite.config.ts 的 @server type-only 约定对此文件开例外——本文件必须保持零依赖。

export type ProtocolLang = 'zh' | 'en';

// ---------- 接棒 ----------

export const BATON_TAGS = {
  zh: '<接棒>',
  /** 旧语法,仅中文(兼容旧 session 记忆惯性) */
  zhLegacy: '【接棒】',
  en: '<pass>',
} as const;

/** 按语言取发射侧标签(prompt 教学用) */
export function batonTagFor(lang: ProtocolLang): string {
  return lang === 'en' ? BATON_TAGS.en : BATON_TAGS.zh;
}

/** 接棒行匹配(双语并集;英文侧大小写不敏感由 flag 处理不了交替——显式双写) */
export const BATON_LINE = /(?:<接棒>|【接棒】|<pass>|<PASS>)\s*(.+)/;

/** 接棒尾行剥除(g 标志,整行删) */
export const BATON_STRIP = /(?:<接棒>|【接棒】|<pass>|<PASS>)[^\n]*/g;

/**
 * 接棒结束关键词(中英语料并集,不区分当前语言)。
 * 英文词必须带 \b:防 "recommend"/"attend" 内嵌 "end" 误触发。
 */
export const BATON_END_WORDS = /结束|收敛|无需|到此|\b(?:end|conclude|wrap)\b/i;

/** @名字提取(截断符:空白、@、中英逗号、中文句号——现状字符类不动) */
export const AT_NAME = /@([^\s@,，。]+)/;
export const AT_NAME_GLOBAL = /@([^\s@,，。]+)/g;

/** 用户/房主代称(接棒目标指向用户时的名字判定;现状已双语) */
export const USER_NAME_ALIASES: string[] = ['用户', 'user'];

// ---------- 沉默/跳过(订阅模式心跳自决) ----------

/** 精确等于即沉默(中英并集) */
export const SILENT_EXACT: readonly string[] = [
  '<跳过>', '【跳过】', '跳过', '<沉默>', '【沉默】', '沉默',
  '<skip>', '<Skip>', '<SKIP>', 'skip',
  '<silent>', '<Silent>', '<SILENT>', 'silent',
];
/** 前缀即沉默(标签后跟了多余说明的容错) */
export const SILENT_PREFIX: readonly string[] = [
  '<跳过>', '【跳过】', '<沉默>', '【沉默】',
  '<skip>', '<Skip>', '<SKIP>', '<silent>', '<Silent>', '<SILENT>',
];

/** 沉默判定唯一真源(空文本/undefined 同判沉默——现状语义) */
export function isSilentText(raw: string | undefined): boolean {
  if (!raw) return true;
  const trimmed = raw.trim();
  if (trimmed.length === 0) return true;
  return (
    SILENT_EXACT.includes(trimmed) ||
    SILENT_PREFIX.some((p) => trimmed.startsWith(p))
  );
}
```

- [ ] **Step 4: 改写 baton.ts 消费真源(行为不变)**

`server/src/core/modes/baton/baton.ts` 全文替换为:

```ts
// 接棒模式: 尾行解析与接棒决策纯函数。
// 协议正则/词表真源在 ../../protocolKeywords(根级零依赖叶子)——双语并集匹配。

import { matchMemberByName } from '../../naming';
import {
  BATON_LINE, BATON_STRIP, BATON_END_WORDS, AT_NAME, USER_NAME_ALIASES,
} from '../../protocolKeywords';

/** 接棒行正则(真源 re-export,既有 import 不动) */
export { BATON_LINE };

export interface BatonOutcome {
  nextMemberId?: string;
  endDiscussion?: boolean;
  toUser?: boolean;
}

/**
 * 接棒尾行解析: 从发言全文末尾提取接棒指令(中英标签并集)。
 * @param text 完整发言文本
 * @param members 房间成员候选列表
 * @param selfId 当前发言者 ID (防传给自己)
 * @param userNames 用户或用户人设名称集合 (默认 USER_NAME_ALIASES)
 */
export function parseBaton(
  text: string,
  members: Array<{ id: string; name: string }>,
  selfId: string,
  userNames: string[] = USER_NAME_ALIASES,
): BatonOutcome {
  // 取最后 3 行内找接棒标记(容错: agent 可能在正文里换行后又补写)
  const tailLines = text.trim().split('\n').slice(-3);
  for (const line of tailLines.reverse()) {
    const m = line.match(BATON_LINE);
    if (!m) continue;
    const directive = (m[1] ?? '').trim();
    if (BATON_END_WORDS.test(directive)) return { endDiscussion: true };
    // @名字 或 直接名字
    const nameMatch = directive.match(AT_NAME);
    const rawName = ((nameMatch?.[1]) ?? directive).trim();

    // 检查是否明确接棒给用户/房主
    if (userNames.some((u) => u && rawName.toLowerCase() === u.toLowerCase())) {
      return { toUser: true };
    }

    const hit = matchMemberByName(rawName, members);
    if (hit && hit.id !== selfId) return { nextMemberId: hit.id };
    if (hit && hit.id === selfId) return {}; // 传给自己: 无效 -> 无指令
    return {}; // 名字对不上: 无效 -> 无指令
  }
  return {}; // 没有接棒行: 无指令
}

/** 剥除发言文本中的接棒尾行(在非接棒模式或非链上发言时使用;双语) */
export function stripBatonLine(text: string): string {
  return text.replace(BATON_STRIP, '').trimEnd();
}
```

- [ ] **Step 5: isSilentDecision 改为委托真源**

`server/src/core/modes/subscribe/prompt.ts`:顶部加 `import { isSilentText } from '../../../protocolKeywords';`,把 `isSilentDecision` 函数体(121-140 行)替换为:

```ts
/**
 * 检查输出是否为跳过或沉默指令(真源:protocolKeywords.isSilentText,中英并集)
 */
export function isSilentDecision(rawText: string | undefined): boolean {
  return isSilentText(rawText);
}
```

(保留导出名,engine.ts/publish.ts 的 import 不动。)

- [ ] **Step 6: store/trace.ts 改引真源**

`server/src/store/trace.ts:266`:`trace.output?.result?.includes('<跳过>')` 改为 `trace.output?.result?.includes('<跳过>') || /<skip>|<silent>/i.test(trace.output?.result ?? '')`——不,直接用真源:

```ts
import { isSilentText } from '../protocolKeywords';
// ...
if (trace.status === 'cancelled' || isSilentText(trace.output?.result?.trim())) {
```

注意:`isSilentText` 对 undefined 判 true——原逻辑是 `includes` 只在 result 存在时可能 true。核对原语义:`trace.output?.result?.includes('<跳过>')` 在 result 为 undefined 时为 undefined(falsy)→ 不计入 skip。用 `isSilentText` 会把空 result 判成 skip,**语义变化**。保持原语义写法:

```ts
const resultText = trace.output?.result;
if (trace.status === 'cancelled' || (resultText !== undefined && isSilentText(resultText.trim()) && resultText.trim().length < 30)) {
```

——过度设计。简单方案(语义严格不变 + 覆盖英文标签):

```ts
if (
  trace.status === 'cancelled' ||
  (trace.output?.result !== undefined && SKIP_TAG_IN_RESULT.test(trace.output.result))
) {
```

并在 protocolKeywords.ts 增加:

```ts
/** trace 统计用:结果文本包含跳过标签(中英并集;不判空文本——统计语义) */
export const SKIP_TAG_IN_RESULT = /<跳过>|【跳过】|<skip>|<silent>/i;
```

trace.ts: `import { SKIP_TAG_IN_RESULT } from '../protocolKeywords';`(store→根叶子运行时 import,合规——store→core 防线未触碰)。

- [ ] **Step 7: 跑全量测试**

Run: `cd server && npm test`
Expected: 全绿(既有 baton/orchestrator/subscribe 测试零改动通过 + 新增 protocol-keywords 通过)。若 subscribe 测试有 `startsWith('<跳过>')` 类断言失败,检查 SILENT_PREFIX 是否漏了某字面量——从旧实现 10 个字面量逐一核对。

- [ ] **Step 8: Commit**

```bash
git add server/src/protocolKeywords.ts server/src/core/modes/baton/baton.ts server/src/core/modes/subscribe/prompt.ts server/src/store/trace.ts server/tests/protocol-keywords.test.ts
git commit -m "feat(i18n): 协议关键字根级叶子收口+接棒/沉默词表双语并集(匹配并集,发射单侧)"
```

### Task 2: audience.ts 私聊/握手/清洗词表双语化

**Files:**
- Modify: `server/src/protocolKeywords.ts`(追加私聊/握手/清洗段)
- Modify: `server/src/core/modes/subscribe/audience.ts`(AUDIENCE_LINE、内联正则 ×7、inferHandshakeFromText 语料)
- Test: `server/tests/protocol-keywords.test.ts`(追加 describe)

**Interfaces:**
- Produces:
  - `export const DM_TAGS = { zh: '<私聊>', zhLegacy: '【私聊】', en: '<dm>' }`
  - `export function dmTagFor(lang: ProtocolLang): string`
  - `export const DM_LINE: RegExp` / `export const DM_TAG_GLOBAL: RegExp`(g)
  - `export const DM_STRIP: RegExp`(g)
  - `export type HandshakeKind = 'agree' | 'reject' | 'idea'`
  - `export const HANDSHAKE_TAGS: Record<HandshakeKind, { zh: string; en: string }>`(`{ agree: {zh:'同意',en:'agree'}, reject: {zh:'拒绝',en:'decline'}, idea: {zh:'想法',en:'idea'} }`)
  - `export function handshakeTagFor(kind: HandshakeKind, lang: ProtocolLang): string`(返回带尖括号完整标签,如 `<agree>`)
  - `export const HANDSHAKE_LINE: RegExp`(并集,捕获词)
  - `export const HANDSHAKE_STRIP: RegExp`(g,并集)
  - `export const HANDSHAKE_TAG_GLOBAL: RegExp`(g,并集,找正文残留)
  - `export function handshakeKindOf(word: string): HandshakeKind | undefined`
  - `export const PRIVATE_PREFIX_STRIP: RegExp`(私聊正文清洗,并集)
  - `export const PUBLIC_PREFIX_STRIP: RegExp`(公聊正文清洗,并集)
  - `export const AT_NAME_LOOSE: RegExp`(audience 内层 `^\s*@名字` 提取,字符类同现状 + 冒号等)
  - `export const INFER_AGREE: RegExp` / `INFER_REJECT` / `INFER_IDEA`(语料并集,i 标志)

- [ ] **Step 1: 写失败测试(追加到 protocol-keywords.test.ts)**

```ts
import {
  splitPublicAndPrivateMessage, parseHandshake, parseAudience,
  inferHandshakeFromText,
} from '../src/core/modes/subscribe/audience';

describe('私聊/握手双语并集解析', () => {
  const members = [
    { id: 'm1', name: '甲' }, { id: 'm2', name: '乙' }, { id: 'm3', name: 'Bob' },
  ];

  it('<dm>@Bob 拆出私聊块(与 <私聊>@乙 等价)', () => {
    const zh = splitPublicAndPrivateMessage('公开说一句\n<私聊>@乙 单独聊', members, 'm1');
    const en = splitPublicAndPrivateMessage('public words\n<dm>@Bob just between us', members, 'm1');
    expect(zh.privateBlocks[0]?.targetMemberIds).toEqual(['m2']);
    expect(en.privateBlocks[0]?.targetMemberIds).toEqual(['m3']);
    expect(en.publicText).toBe('public words');
    expect(en.privateBlocks[0]?.privateText).toBe('just between us');
  });
  it('<agree>/<decline> 握手标签解析', () => {
    expect(parseHandshake('成交\n<同意>', members)?.type).toBe('agree');
    expect(parseHandshake('deal\n<agree>', members)?.type).toBe('agree');
    expect(parseHandshake('no\n<decline>', members)?.type).toBe('reject');
    expect(parseHandshake('but\n<idea> @甲', members)).toEqual({ type: 'idea', targetMemberId: 'm1' });
  });
  it('英文语义兜底推断', () => {
    expect(inferHandshakeFromText('Deal.')).toBe('agree');
    expect(inferHandshakeFromText('Sure, count me in.')).toBe('agree');
    expect(inferHandshakeFromText('No way.')).toBe('reject');
    expect(inferHandshakeFromText('Absolutely not.')).toBe('reject');
    expect(inferHandshakeFromText('But my condition is…')).toBe('idea');
    expect(inferHandshakeFromText('However, I propose…')).toBe('idea');
  });
  it('英文清洗前缀(private:/public: 剥除)', () => {
    const r = splitPublicAndPrivateMessage('public: hi all\n<dm>@Bob private: secret', members, 'm1');
    expect(r.publicText).toBe('hi all');
    expect(r.privateBlocks[0]?.privateText).toBe('secret');
  });
  it('parseAudience <dm> 尾行解析', () => {
    expect(parseAudience('话说完\n<dm>@Bob', members, 'm1')).toEqual(['m3']);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd server && npx vitest run tests/protocol-keywords.test.ts`
Expected: FAIL(`<dm>` 不被识别)

- [ ] **Step 3: protocolKeywords.ts 追加私聊/握手段**

```ts
// ---------- 私聊(订阅模式) ----------

export const DM_TAGS = {
  zh: '<私聊>',
  zhLegacy: '【私聊】',
  en: '<dm>',
} as const;

export function dmTagFor(lang: ProtocolLang): string {
  return lang === 'en' ? DM_TAGS.en : DM_TAGS.zh;
}

/** 私聊行匹配(双语并集;英文大小写双写) */
export const DM_LINE = /(?:<私聊>|【私聊】|<dm>|<DM>)\s*(.+)/;
/** 私聊标签全文扫描(切 chunk 用,g 标志) */
export const DM_TAG_GLOBAL = /(?:<私聊>|【私聊】|<dm>|<DM>)/g;
/** 私聊尾行剥除(g 标志,整行删) */
export const DM_STRIP = /(?:<私聊>|【私聊】|<dm>|<DM>)[^\n]*/g;

// ---------- 握手(私聊态度三件套) ----------

export type HandshakeKind = 'agree' | 'reject' | 'idea';

export const HANDSHAKE_TAGS: Record<HandshakeKind, { zh: string; en: string }> = {
  agree: { zh: '同意', en: 'agree' },
  reject: { zh: '拒绝', en: 'decline' },
  idea: { zh: '想法', en: 'idea' },
};

/** 按语言取发射侧握手标签(完整尖括号形式,如 '<agree>') */
export function handshakeTagFor(kind: HandshakeKind, lang: ProtocolLang): string {
  const t = HANDSHAKE_TAGS[kind];
  return `<${lang === 'en' ? t.en : t.zh}>`;
}

/** 握手行匹配(双语并集;捕获词用于 kind 判定) */
export const HANDSHAKE_LINE = /<(同意|拒绝|想法|agree|decline|idea|AGREE|DECLINE|IDEA)>\s*(.*)/;
/** 握手标签剥除(g 标志,整行删) */
export const HANDSHAKE_STRIP = /<(?:同意|拒绝|想法|agree|decline|idea|AGREE|DECLINE|IDEA)>[^\n]*/g;

export function handshakeKindOf(word: string): HandshakeKind | undefined {
  const w = word.toLowerCase();
  for (const kind of Object.keys(HANDSHAKE_TAGS) as HandshakeKind[]) {
    const t = HANDSHAKE_TAGS[kind];
    if (w === t.zh || w === t.en) return kind;
  }
  return undefined;
}

// ---------- 正文清洗前缀(AI 输出残留标签词;中英并集) ----------

/** 私聊正文开头的残留前缀(如 "私聊: xxx" / "private: xxx") */
export const PRIVATE_PREFIX_STRIP = /^\s*(?:私聊(?:部分)?|悄悄话|密谋|private|dm)[:：\s]*/i;
/** 公聊正文开头的残留前缀(如 "公开发言: xxx" / "public: xxx") */
export const PUBLIC_PREFIX_STRIP = /^\s*(?:公开发言(?:部分)?|公开回复|台前发言|公聊(?:部分)?|public(?: reply| speech| statement)?|on stage)[:：\s]*/i;

// ---------- @名字(audience 内层;字符类同 AT_NAME,额外容忍冒号截断,与现状一致) ----------

export const AT_NAME_LOOSE = /^([^\s@,，。:：\n]+)/;

// ---------- 语气语义兜底(无显式标签时;中英语料并集,不论当前语言) ----------

/** 同意/结盟倾向(行首整词;英文词带 \b 防内嵌) */
export const INFER_AGREE = /^(?:(?:好|可以|行|成|没问题|成交|一言为定|依你|就依你|听你的|赞同|认同|同意|接受|按你说的办|依你说的办|sure|ok|okay|deal|agreed|i agree|accepted|fine by me|count me in)[\s，。！!：:]*|.*(?:一言为定|达成同盟|结盟已成|此约已定|此约便定|\bdeal\b|\b(it's a deal|deal is done)\b))/i;
/** 拒绝倾向 */
export const INFER_REJECT = /^(?:(?:不行|拒绝|休想|不妥|免谈|绝不|恕难从命|我不同意|不答应|no way|refuse|rejected|i decline|i disagree|absolutely not|out of the question)[\s，。！!：:]*|.*(?:绝无可能|绝不同意|绝不答应|断难从命|\bnever\b))/i;
/** 想法/反议价/补充条件倾向 */
export const INFER_IDEA = /^(?:(?:不过|但|我有不同想法|我的条件是|不如这样|另有一言|however|but|i have a (?:different idea|counterproposal)|my condition is|what if|counteroffer)[\s，。！!：:]*)/i;
```

- [ ] **Step 4: audience.ts 全部内联正则改引真源**

`server/src/core/modes/subscribe/audience.ts` 逐处替换(保持逻辑结构不动):

1. `:7` `export const AUDIENCE_LINE = /(?:<私聊>|【私聊】)\s*(.+)/;` → `export { DM_LINE as AUDIENCE_LINE } from '../../../protocolKeywords';`(改为 re-export;文件内使用处 `import { DM_LINE as AUDIENCE_LINE, ... } from '../../../protocolKeywords'` 一并引入)
2. `:46` `privateTagRegex` 内联 → `const matches = [...rawText.matchAll(DM_TAG_GLOBAL)];`(用前先 `DM_TAG_GLOBAL.lastIndex = 0`——g 正则 matchAll 自重置,安全)
3. `:89` `/^\s*@([^\s@,，。:：\n]+)/` → 保留原样(它是 chunk 内层提取,与 AT_NAME_LOOSE 字符类一致即可):`new RegExp('^\\s*@([\\\\s@,，。:：\\\\n]+)'.replace(...))` ——**不要这么绕**。直接在该文件用 `AT_NAME` 不行(字符类少了 `:：\n`)。方案:protocolKeywords 增加 `export const AT_NAME_STRICT_LOOSE = /^\s*@([^\s@,，。:：\n]+)/;`——命名冗长。**定名**:`export const AT_TARGET = /^\s*@([^\s@,，。:：\n]+)/;`,audience `:89` 与 `:101`(singleMatch 用 `^([^\s@,，。:：\n]+)`)分别引用 `AT_TARGET` 与 `AT_NAME_LOOSE`。
4. `:121` `chunk.match(/<(同意|拒绝|想法)>/)` → `chunk.match(HANDSHAKE_LINE)`,类型判定 `handshakeKindOf(m[1]!)`
5. `:133-134` 清洗链 → `.replace(HANDSHAKE_STRIP, '').replace(PRIVATE_PREFIX_STRIP, '')`(注意原实现 PRIVATE 前缀正则带 g?原:`/^\s*(?:私聊(?:部分)?|悄悄话|密谋)[:：\s]*/g` 有 g 但 ^ 锚定单次等价,新 PRIVATE_PREFIX_STRIP 无 g 等价)
6. `:150` 公聊清洗 → `.replace(PUBLIC_PREFIX_STRIP, '')`(原 g 同上等价)
7. `:165` `/@?([^\s@,，。:：\n]+)/` → `new RegExp(...)` 不引;该处容错提取直接用 `AT_NAME_LOOSE` 变体:`tailChunk?.match(/@?([^\s@,，。:：\n]+)/)` 改为匹配 `('@' + AT_NAME_LOOSE.source)`?**简化**:protocolKeywords 增加 `export const AT_NAME_OPTIONAL = /@?([^\s@,，。:：\n]+)/;`
8. `:169` `rawText.match(/<(同意|拒绝|想法)>/)` → `HANDSHAKE_LINE` + `handshakeKindOf`
9. `:176` `.replace(/<(?:同意|拒绝|想法)>[^\n]*/g, '')` → `.replace(HANDSHAKE_STRIP, '')`
10. `:213` `directive.matchAll(/@([^\s@,，。]+)/g)` → `directive.matchAll(AT_NAME_GLOBAL)`
11. `:265` `HANDSHAKE_LINE` 本地定义 → re-export 真源
12. `:283-284` `if (rawType === '拒绝')...` → `const kind = handshakeKindOf(rawType!) ?? 'agree';` 映射
13. `:289` `targetStr.match(/@([^\s@,，。]+)/)` → `AT_NAME`
14. `:304-308` `stripAudienceLine` → `.replace(DM_STRIP, '').replace(HANDSHAKE_STRIP, '')`
15. `:232-254` `inferHandshakeFromText` 三段正则 → `INFER_AGREE/INFER_REJECT/INFER_IDEA`:

```ts
export function inferHandshakeFromText(text: string): 'agree' | 'reject' | 'idea' | undefined {
  if (!text) return undefined;
  const trimmed = text.trim();
  if (INFER_AGREE.test(trimmed)) return 'agree';
  if (INFER_REJECT.test(trimmed)) return 'reject';
  if (INFER_IDEA.test(trimmed)) return 'idea';
  return undefined;
}
```

(保留该函数在 audience.ts——historyOps.ts import 它,不动消费方。)

- [ ] **Step 5: 跑全量测试**

Run: `cd server && npm test`
Expected: 全绿。若 subscribe.test 有依赖"英文正文不触发推断"的用例因语料变宽而失败,逐例核对:推断只在**无显式标签**路径触发,显式标签优先级不变。

- [ ] **Step 6: Commit**

```bash
git add server/src/protocolKeywords.ts server/src/core/modes/subscribe/audience.ts server/tests/protocol-keywords.test.ts
git commit -m "feat(i18n): 私聊/握手/清洗词表双语并集收口至 protocolKeywords 真源"
```

### Task 3: orchestrator/trace 内联协议正则收口

**Files:**
- Modify: `server/src/core/orchestrator.ts:1012`(parseUserCommand 内联接棒正则)、`:1020`(@all 正则截断符——不动,仅注释)、`:1024`(atNames matchAll)、`:729`(userNames 数组)
- Test: 既有 orchestrator.test.ts 继续通过;protocol-keywords.test.ts 架构 tripwire 已覆盖

**Interfaces:**
- Consumes: Task 1 的 `BATON_LINE / AT_NAME_GLOBAL / USER_NAME_ALIASES`

- [ ] **Step 1: 替换内联正则**

`orchestrator.ts` 顶部 `import { BATON_LINE, AT_NAME_GLOBAL, USER_NAME_ALIASES } from '../protocolKeywords';`

1. `:1012` `const startMatch = text.match(/(?:<接棒>|【接棒】)\s*@([^\s@,，。]+)/);` → `const startMatch = text.match(BATON_LINE);` 且名字提取改为:

```ts
if (startMatch) {
  const directive = (startMatch[1] ?? '').trim();
  const nameHit = directive.match(AT_NAME);
  const rawName = (nameHit?.[1] ?? directive).trim();
  const hit = rawName ? matchMemberByName(rawName, this.deps.room.members) : undefined;
  if (hit) return { kind: 'start', member: hit, fromName: this.deps.room.userPersona?.name || '用户' };
  return { kind: 'none' };
}
```

(`import { AT_NAME } from '../protocolKeywords';` 一并加。原实现直接用捕获组当名字,新实现走 directive+AT_NAME 与 parseBaton 同构——用户输入 `<接棒>@乙 xxx` 时原实现捕获 `乙`(字符类截断),新实现同截断,行为一致;`<pass>@Bob` 现在也能起手 ✓)

2. `:729` `const userNames = ['用户', 'user'];` → `const userNames = [...USER_NAME_ALIASES];`
3. `:1024` `[...text.matchAll(/@([^\s@,，。]+)/g)]` → `[...text.matchAll(AT_NAME_GLOBAL)]`
4. `:1020` @all 正则保留原样(保留字负向前瞻逻辑特殊,不并入通用 AT_NAME;加注释 `// @allN 保留字:与 protocolKeywords 无关的独立语法`)

- [ ] **Step 2: 跑全量测试**

Run: `cd server && npm test`
Expected: 全绿(orchestrator 29 场景 + baton + 新增)。

- [ ] **Step 3: Commit**

```bash
git add server/src/core/orchestrator.ts
git commit -m "refactor(i18n): orchestrator 内联协议正则收口至 protocolKeywords 真源"
```

---

## Slice 2:语言基建(lang 类型 + settings + API)

### Task 4: core/i18n/lang.ts + messages.ts 词典骨架

**Files:**
- Create: `server/src/core/i18n/lang.ts`
- Create: `server/src/core/i18n/messages.ts`
- Test: `server/tests/lang-messages.test.ts`(新建)

**Interfaces:**
- Produces:
  - `export type Lang = 'zh' | 'en'`(lang.ts)
  - `export type LangGetter = () => Lang`
  - `export function t(lang: Lang, key: string, params?: Record<string, string | number>): string`(messages.ts;zh 兜底;key 不存在返回 key 本身)
  - `export const MESSAGES: { zh: MessageDict; en: MessageDict }`(类型 `Record<string, (p?: any) => string>`)

- [ ] **Step 1: 写失败测试**

```ts
// server/tests/lang-messages.test.ts
import { describe, it, expect } from 'vitest';
import { MESSAGES, t } from '../src/core/i18n/messages';

describe('messages 词典契约', () => {
  it('zh/en 同 key 集(漏译即失败)', () => {
    expect(Object.keys(MESSAGES.en).sort()).toEqual(Object.keys(MESSAGES.zh).sort());
  });
  it('t() zh 渲染与现状逐字节一致(样例)', () => {
    expect(t('zh', 'sys.stoppedThinking')).toBe('(已停止思考)');
    expect(t('zh', 'room.memberLeft', { name: '甲' })).toBe('甲 离开了房间');
    expect(t('zh', 'orch.batonPass', { name: '甲', next: '乙' })).toBe('🎯 甲 把接棒交给 乙');
  });
  it('t() en 渲染', () => {
    expect(t('en', 'sys.stoppedThinking')).toBe('(stopped thinking)');
    expect(t('en', 'room.memberLeft', { name: 'A' })).toBe('A left the room');
  });
  it('未知 key 回退 zh 再回退 key 本身', () => {
    expect(t('en', 'no.such.key')).toBe('no.such.key');
  });
});
```

- [ ] **Step 2: 确认失败**

Run: `cd server && npx vitest run tests/lang-messages.test.ts`
Expected: FAIL(module not found)

- [ ] **Step 3: 写 lang.ts**

```ts
// 语言类型与注入契约(core/i18n 叶子;零运行时依赖)。
// 服务层(orchestrator/room/direct/admin/routes)经 LangGetter 注入读取;
// 纯函数层(prompt/render 组装器)显式 lang 参数——不引入 provider 依赖。

export type Lang = 'zh' | 'en';

export type LangGetter = () => Lang;

/** 未注入时的缺省(全局不变量:与改造前行为逐字节一致) */
export const DEFAULT_LANG: Lang = 'zh';
```

- [ ] **Step 4: 写 messages.ts(zh 全量/en 全量)**

zh 侧字符串**逐字节拷贝自当前源码**(行号锚点在注释里);en 侧按下表翻译。完整清单:

```ts
// A 类文案词典:会广播给前端/REST 响应展示的系统消息、错误、显示名、trace 标签。
// zh 侧为现状原文逐字节拷贝(既有测试断言的锁);en 侧翻译。
// t() 兜底链:lang → zh → key 本身。
import type { Lang } from './lang';

type MsgParams = Record<string, string | number>;
type MessageDict = Record<string, (p?: MsgParams) => string>;

export const MESSAGES: { zh: MessageDict; en: MessageDict } = {
  zh: {
    // ---- 通用系统名/占位(orchestrator:631/671, direct:321/364, room:464, direct:209) ----
    'sys.speaker': () => '系统',
    'sys.user': () => '用户',
    'sys.scout': () => '🔍 侦察员',
    'sys.stoppedThinking': () => '(已停止思考)',
    'sys.noOutput': () => '(无输出)',
    'sys.unknownError': () => '未知错误',
    'sys.adapterMissing': (p) => `适配器未配置: ${p?.key ?? ''}`,

    // ---- orchestrator 系统消息(sysNotice/sysMessage) ----
    'orch.wokeQueued': (p) => `已唤醒 ${p?.names ?? ''}，将依次排队回应`,
    'orch.mentionNotFound': () => '没有找到 @ 的成员,控制权回到你手里',
    'orch.mentionReplaced': (p) => `已取消之前的指令,@${p?.name ?? ''} 将回应你并指定下一位`,
    'orch.continueRandom': (p) => `讨论继续, 随机唤醒 ${p?.name ?? ''} 起头回应`,
    'orch.startRandom': (p) => `讨论开始, 随机唤醒 ${p?.name ?? ''} 起头发言`,
    'orch.rerollNotFound': (p) => `找不到重roll成员: ${p?.id ?? ''}`,
    'orch.roundsStart': (p) => `开始轮流发言 ${p?.rounds ?? 1} 轮`,
    'orch.speakFailed': (p) => `${p?.name ?? ''} 发言失败: ${p?.error ?? ''}`,
    'orch.autoBudgetReached': () => '讨论已达自动发言上限,发条新消息可继续。',
    'orch.discussionEnd': (p) => `🏁 ${p?.name ?? ''} 宣布讨论结束。`,
    'orch.handBack': (p) => `🤝 ${p?.name ?? ''} 把话题交还给了你。`,
    'orch.noNext': (p) => `${p?.name ?? ''} 没有指定下一位,控制权回到你手中。发消息将从随机成员继续。`,
    'orch.pendingStandby': (p) => `⏸ ${p?.name ?? ''} 指定 ${p?.next ?? ''} 接棒。你发消息后 TA 开始发言。`,
    'orch.batonBudgetReached': () => '自由讨论已达接棒上限,发条新消息可继续。',
    'orch.batonPass': (p) => `🎯 ${p?.name ?? ''} 把接棒交给 ${p?.next ?? ''}`,
    'orch.roundsEnd': () => '轮流发言结束。可继续 @成员 追问或发消息自由讨论。',

    // ---- orchestrator trigger(trace 面板展示;原文自 :298/:338/:371/:398/:424/:433/:500/:519-533/:803,逐字节拷贝) ----
    'orch.trigger.designatedStart': (p) => `${p?.fromName ?? ''} 指定你起头,请就主题开个头或回应该消息。`,
    'orch.trigger.mentioned': (p) => `用户在聊天中 @了你,请回应用户。回应完在结尾用 ${p?.batonTag ?? '<接棒>'}@名字 指定下一位(讨论将暂停等待用户)。`,
    'orch.trigger.wokenByUser': (p) => `用户唤醒你回应讨论,请回应用户或参与讨论。`,
    'orch.trigger.coldStart': (p) => `用户发起讨论,请你起头发言,开场点明你的立场。`,
    'orch.trigger.starterReason': (p) => `${p?.reason ?? ''},请你率先发言,亮明你的立场。`,
    'orch.trigger.userDirect': (p) => `用户(房间主人)直接对你说:${p?.text ?? ''}\n回应完在结尾用 ${p?.batonTag ?? '<接棒>'}@名字 指定下一位(讨论将暂停等待用户)。`,
    'orch.trigger.rounds': (p) => `轮流发言(第 ${p?.i ?? 1}/${p?.n ?? 1} 位),请就当前主题发表你的观点。`,
    'orch.trigger.batonToYou': (p) => `${p?.name ?? ''} 指定你接棒。请针对 TA 刚才的发言回应、反驳或补充。`,

    // ---- room ----
    'room.memberJoined': (p) => `${p?.names ?? ''} 加入了房间,当前 ${p?.count ?? 0} 位成员`,
    'room.memberLeft': (p) => `${p?.name ?? ''} 离开了房间`,
    'room.memberMissing': (p) => `成员不存在: ${p?.id ?? ''}`,
    'room.needMembersFirst': () => '房间里还没有成员,请先添加成员再开始。',
    'room.startSubscribe': () => '自由讨论开始(订阅模式): 依据全员发言意愿驱动讨论。',
    'room.startBaton': () => '自由讨论开始(接棒模式):有待命接棒者由 TA 起头,否则随机。',
    'room.defaultName': () => '新房间',
    'room.defaultTopic': () => '自由聊天',
    'room.adminAdapterMissing': (p) => `管理员/侦察适配器未配置: ${p?.key ?? ''}(检查 config/agents.yaml 的 admin.adapter 与 adapters 是否一致)`,

    // ---- room/direct trace 标签 ----
    'trace.scoutExplore': () => '项目目录勘探',
    'trace.summaryDistill': () => '讨论大纲提炼',
    'trace.privateDigest': () => '私聊纪要自总结',
    'trace.heartbeat': () => '心跳自主发言',
    'trace.directChat': () => '1v1用户对话',
    'trace.pendingReason': (p) => `${p?.name ?? ''} 是之前被指定的接棒对象`,
    'trace.coldStartPicked': () => '冷启动随机选中',

    // ---- subscribe engine/publish ----
    'sub.silentSkip': (p) => `${p?.name ?? ''} 评估暂无发言与私聊意向，选择跳过。`,
    'sub.skipViolated': (p) => `${p?.name ?? ''} 尝试跳过发言(点名场景不允许跳过),本次发言已忽略。`,
    'sub.dmMention': (p) => `同事 ${p?.name ?? ''} 在私聊中对你提出了新想法，请回应。`,
    'sub.atMention': (p) => `同事 ${p?.name ?? ''} 在发言中点名提及了你。`,

    // ---- direct ----
    'direct.replyFailed': (p) => `${p?.name ?? ''} 回复失败: ${p?.error ?? ''}`,

    // ---- admin ----
    'admin.scoutTrigger': () => '开场侦察:分析项目并播报,供全员讨论使用',
    'admin.summaryBreaker': () => '管理员摘要服务暂时不可用(连续失败已熔断)。',
    'admin.breakerError': () => '熔断保护中',
    'admin.directTopic': (p) => `与 ${p?.name ?? ''} 的一对一私聊探讨`,

    // ---- ws ----
    'ws.hello': () => 'AI 聊天室已连接',

    // ---- REST/routes(错误消息;原文自 routes.ts 各行) ----
    'api.requestFailed': (p) => `请求失败(${p?.status ?? 0})`,  // web 侧用,后端 t 不含此 key 也可
    'api.uploadTooLarge': () => '上传文件超出最大限制 (15MB)',
    'api.roomFileNotCharacter': () => '该文件为房间配置文件，无法作为角色卡导入',
    'api.characterFileNotRoom': () => '该文件为角色卡，无法作为房间配置导入',
    'api.roomJsonInvalid': () => '房间配置文件必须是合法的 JSON 格式',
    'api.roomStructInvalid': () => '无效的房间配置结构',
    'api.unnamedRoom': () => '未命名房间',
    'api.memberN': (p) => `成员${p?.n ?? 1}`,
    'api.member': () => '成员',
    'api.charNeedNamePersona': () => '角色需要名字和人设',
    'api.unknownAdapter': (p) => `未知适配器: ${p?.key ?? ''}`,
    'api.unknownAdapterCfg': (p) => `未知适配器配置: ${p?.key ?? ''}`,
    'api.emptyUpload': () => '上传内容为空',
    'api.unrecognizedFile': () => '无法识别的文件类型，既非角色卡亦非房间配置',
    'api.importFailed': () => '导入失败',
    'api.charCardParseFailed': () => '角色卡解析失败',
    'api.charNotFound': () => '角色不存在',
    'api.emptyMessage': () => '空消息',
    'api.editTextEmpty': () => '编辑文本不能为空',
    'api.traceNotFound': () => '未找到调用日志',
    'api.summaryNotFound': () => '未找到摘要快照',
    'api.roomNotFound': (p) => `房间不存在: ${p?.id ?? ''}`,
    'api.noValidCharacters': () => '没有有效角色',
    'api.projectDirMissing': (p) => `项目目录不存在: ${p?.path ?? ''}`,
    'api.needMemberAndText': () => '缺 memberId 或 text',
    'api.needMembersArray': () => '缺 members 数组',
    'api.instructionFailed': (p) => `指令执行失败: ${p?.msg ?? ''}`,
    'api.orchStartFailed': (p) => `编排启动失败: ${p?.msg ?? ''}`,
    'api.updateSettingsFailed': () => '更新房间设置失败',
    'api.noSuchRoute': (p) => `无此路由: ${p?.method ?? ''} ${p?.path ?? ''}`,
    'api.badLang': () => 'lang 只能是 zh 或 en',

    // ---- historyOps / sessionGuard(core 抛错→REST 展示) ----
    'ho.msgNotFound': (p) => `未找到指定消息: ${p?.id ?? ''}`,
    'ho.aiOnly': () => '只能对 AI 成员的发言执行重roll',
    'sg.personaLocked': () => '当前会话已有消息记录，身份设定已锁定。如需更换请先清空历史消息。',
  },
  en: {
    'sys.speaker': () => 'System',
    'sys.user': () => 'User',
    'sys.scout': () => '🔍 Scout',
    'sys.stoppedThinking': () => '(stopped thinking)',
    'sys.noOutput': () => '(no output)',
    'sys.unknownError': () => 'unknown error',
    'sys.adapterMissing': (p) => `Adapter not configured: ${p?.key ?? ''}`,

    'orch.wokeQueued': (p) => `Woke up ${p?.names ?? ''}; they will respond in turn`,
    'orch.mentionNotFound': () => 'No matching @ member; control returns to you',
    'orch.mentionReplaced': (p) => `Previous instructions cancelled. @${p?.name ?? ''} will respond to you and pick the next speaker`,
    'orch.continueRandom': (p) => `Discussion continues — ${p?.name ?? ''} randomly woken to open the response`,
    'orch.startRandom': (p) => `Discussion starts — ${p?.name ?? ''} randomly woken to open`,
    'orch.rerollNotFound': (p) => `Cannot re-roll member: ${p?.id ?? ''}`,
    'orch.roundsStart': (p) => `Round-robin begins: ${p?.rounds ?? 1} round(s)`,
    'orch.speakFailed': (p) => `${p?.name ?? ''} failed to speak: ${p?.error ?? ''}`,
    'orch.autoBudgetReached': () => 'Auto-speech budget reached. Send a new message to continue.',
    'orch.discussionEnd': (p) => `🏁 ${p?.name ?? ''} declared the discussion closed.`,
    'orch.handBack': (p) => `🤝 ${p?.name ?? ''} handed the topic back to you.`,
    'orch.noNext': (p) => `${p?.name ?? ''} did not pick a next speaker; control returns to you. Send a message to continue with a random member.`,
    'orch.pendingStandby': (p) => `⏸ ${p?.name ?? ''} passed the baton to ${p?.next ?? ''}. TA speaks after your next message.`,
    'orch.batonBudgetReached': () => 'Baton chain budget reached. Send a new message to continue.',
    'orch.batonPass': (p) => `🎯 ${p?.name ?? ''} passed the baton to ${p?.next ?? ''}`,
    'orch.roundsEnd': () => 'Round-robin finished. Keep @-mentioning members or send messages for free discussion.',

    'orch.trigger.designatedStart': (p) => `${p?.fromName ?? ''} designated you to open. Please kick off the topic or respond to the message.`,
    'orch.trigger.mentioned': (p) => `The user @-mentioned you in chat. Respond to the user, then pick the next speaker with ${p?.batonTag ?? '<pass>'}@name at the end (discussion pauses until the user speaks).`,
    'orch.trigger.wokenByUser': (p) => `The user woke you to respond to the discussion. Please respond to the user or join in.`,
    'orch.trigger.coldStart': (p) => `The user started the discussion. Please open with your position.`,
    'orch.trigger.starterReason': (p) => `${p?.reason ?? ''}. You speak first — state your position.`,
    'orch.trigger.userDirect': (p) => `The user (room owner) says directly to you: ${p?.text ?? ''}\nAfter responding, pick the next speaker with ${p?.batonTag ?? '<pass>'}@name at the end (discussion pauses until the user speaks).`,
    'orch.trigger.rounds': (p) => `Round-robin (speaker ${p?.i ?? 1} of ${p?.n ?? 1}). Please state your view on the topic.`,
    'orch.trigger.batonToYou': (p) => `${p?.name ?? ''} passed the baton to you. Respond to, rebut, or build on TA last speech.`,

    'room.memberJoined': (p) => `${p?.names ?? ''} joined the room — ${p?.count ?? 0} members now`,
    'room.memberLeft': (p) => `${p?.name ?? ''} left the room`,
    'room.memberMissing': (p) => `Member not found: ${p?.id ?? ''}`,
    'room.needMembersFirst': () => 'The room has no members yet. Add members before starting.',
    'room.startSubscribe': () => 'Free discussion starts (subscribe mode): driven by everyone\'s willingness to speak.',
    'room.startBaton': () => 'Free discussion starts (baton mode): pending baton holder opens; otherwise random.',
    'room.defaultName': () => 'New Room',
    'room.defaultTopic': () => 'Free chat',
    'room.adminAdapterMissing': (p) => `Admin/scout adapter not configured: ${p?.key ?? ''} (check admin.adapter vs adapters in config/agents.yaml)`,

    'trace.scoutExplore': () => 'Project directory scouting',
    'trace.summaryDistill': () => 'Discussion outline distillation',
    'trace.privateDigest': () => 'Private DM self-digest',
    'trace.heartbeat': () => 'Heartbeat self-initiated speech',
    'trace.directChat': () => '1v1 user chat',
    'trace.pendingReason': (p) => `${p?.name ?? ''} was the previously designated baton holder`,
    'trace.coldStartPicked': () => 'randomly picked at cold start',

    'sub.silentSkip': (p) => `${p?.name ?? ''} assessed no intent to speak or DM — skipping.`,
    'sub.skipViolated': (p) => `${p?.name ?? ''} tried to skip (not allowed when called out); this speech was ignored.`,
    'sub.dmMention': (p) => `Colleague ${p?.name ?? ''} raised a new idea with you in a DM — please respond.`,
    'sub.atMention': (p) => `Colleague ${p?.name ?? ''} @-mentioned you in their speech.`,

    'direct.replyFailed': (p) => `${p?.name ?? ''} failed to reply: ${p?.error ?? ''}`,

    'admin.scoutTrigger': () => 'Opening scout: analyze the project and broadcast for the discussion',
    'admin.summaryBreaker': () => 'Admin summary service unavailable (breaker tripped after repeated failures).',
    'admin.breakerError': () => 'breaker open',
    'admin.directTopic': (p) => `One-on-one private discussion with ${p?.name ?? ''}`,

    'ws.hello': () => 'AI Chatroom connected',

    'api.requestFailed': (p) => `Request failed (${p?.status ?? 0})`,
    'api.uploadTooLarge': () => 'Upload exceeds the 15MB limit',
    'api.roomFileNotCharacter': () => 'This is a room config file and cannot be imported as a character card',
    'api.characterFileNotRoom': () => 'This is a character card and cannot be imported as a room config',
    'api.roomJsonInvalid': () => 'Room config must be valid JSON',
    'api.roomStructInvalid': () => 'Invalid room config structure',
    'api.unnamedRoom': () => 'Unnamed Room',
    'api.memberN': (p) => `Member ${p?.n ?? 1}`,
    'api.member': () => 'Member',
    'api.charNeedNamePersona': () => 'Character needs a name and a persona',
    'api.unknownAdapter': (p) => `Unknown adapter: ${p?.key ?? ''}`,
    'api.unknownAdapterCfg': (p) => `Unknown adapter config: ${p?.key ?? ''}`,
    'api.emptyUpload': () => 'Upload is empty',
    'api.unrecognizedFile': () => 'Unrecognized file type: neither a character card nor a room config',
    'api.importFailed': () => 'Import failed',
    'api.charCardParseFailed': () => 'Failed to parse character card',
    'api.charNotFound': () => 'Character not found',
    'api.emptyMessage': () => 'Empty message',
    'api.editTextEmpty': () => 'Edit text cannot be empty',
    'api.traceNotFound': () => 'Trace log not found',
    'api.summaryNotFound': () => 'Summary snapshot not found',
    'api.roomNotFound': (p) => `Room not found: ${p?.id ?? ''}`,
    'api.noValidCharacters': () => 'No valid characters',
    'api.projectDirMissing': (p) => `Project directory does not exist: ${p?.path ?? ''}`,
    'api.needMemberAndText': () => 'Missing memberId or text',
    'api.needMembersArray': () => 'Missing members array',
    'api.instructionFailed': (p) => `Instruction failed: ${p?.msg ?? ''}`,
    'api.orchStartFailed': (p) => `Orchestration failed to start: ${p?.msg ?? ''}`,
    'api.updateSettingsFailed': () => 'Failed to update room settings',
    'api.noSuchRoute': (p) => `No such route: ${p?.method ?? ''} ${p?.path ?? ''}`,
    'api.badLang': () => 'lang must be zh or en',

    'ho.msgNotFound': (p) => `Message not found: ${p?.id ?? ''}`,
    'ho.aiOnly': () => 'Re-roll is only allowed on AI member speeches',
    'sg.personaLocked': () => 'This session already has messages; the persona is locked. Clear history to change it.',
  },
};

/** 渲染一条消息:lang → zh → key 本身兜底 */
export function t(lang: Lang, key: string, params?: Record<string, string | number>): string {
  const fn = MESSAGES[lang]?.[key] ?? MESSAGES.zh[key];
  if (!fn) return key;
  return fn(params);
}
```

⚠️ **zh 侧拷贝纪律**:`orch.trigger.*` 的 zh 文案在实现时必须打开 orchestrator.ts 对应行逐字核对(上表为最接近版本;`:371/:398/:424/:433/:519-533` 的原文以上表为草稿、以源码为准)。trigger 里嵌的接棒标签改为 `${p?.batonTag}` 参数,调用点传 `batonTagFor(this.lang)`——**zh 侧渲染结果必须与现状逐字节一致**(`batonTagFor('zh') === '<接棒>'`)。

- [ ] **Step 5: 跑词典测试**

Run: `cd server && npx vitest run tests/lang-messages.test.ts && npm test`
Expected: 全绿。

- [ ] **Step 6: Commit**

```bash
git add server/src/core/i18n server/tests/lang-messages.test.ts
git commit -m "feat(i18n): core/i18n 语言叶子 + A 类消息双语词典(zh 逐字节拷贝,en 翻译)"
```

### Task 5: store/settings.ts + REST settings 端点 + 全链注入

**Files:**
- Create: `server/src/store/settings.ts`
- Modify: `server/src/server/routes.ts`(新增 2 端点;createRoutes 签名不变——settings 从模块单例取)
- Modify: `server/src/server/ws.ts`(hello 双语)
- Modify: `server/src/server/index.ts`(loadSettings + 注入 getLang)
- Test: `server/tests/settings.test.ts`(新建)

**Interfaces:**
- Produces:
  - `export interface SettingsStore { load(): Promise<void>; getLang(): Lang; setLang(lang: Lang): Promise<void>; }`
  - `export function createSettingsStore(file?: string): SettingsStore`
  - `export const settings: SettingsStore`(进程单例,index.ts load 后全程序用)
  - REST:`GET /api/settings` → `{ lang }`;`PUT /api/settings/language` body `{ lang: 'zh'|'en' }` → `{ lang }` / 400 `{ error }`
- Consumes: Task 4 的 `Lang`(type-only import——store→core 仅 type,合规)

- [ ] **Step 1: 写失败测试**

```ts
// server/tests/settings.test.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { mkdtempSync, rmSync, existsSync, readFileSync } from 'node:fs';
import { tmpdir } from 'node:os';
import path from 'node:path';
import { createSettingsStore } from '../src/store/settings';

let dir: string;
beforeEach(() => {
  dir = mkdtempSync(path.join(tmpdir(), 'aichat-settings-'));
  return () => rmSync(dir, { recursive: true, force: true });
});

describe('settings store', () => {
  it('默认 zh(文件不存在)', async () => {
    const s = createSettingsStore(path.join(dir, 'settings.json'));
    await s.load();
    expect(s.getLang()).toBe('zh');
  });
  it('setLang 持久化且重载恢复', async () => {
    const file = path.join(dir, 'settings.json');
    const s = createSettingsStore(file);
    await s.load();
    await s.setLang('en');
    expect(s.getLang()).toBe('en');
    expect(JSON.parse(readFileSync(file, 'utf8'))).toEqual({ lang: 'en' });
    const s2 = createSettingsStore(file);
    await s2.load();
    expect(s2.getLang()).toBe('en');
  });
  it('损坏文件回退 zh 不断言', async () => {
    const file = path.join(dir, 'settings.json');
    const { writeFileSync } = await import('node:fs');
    writeFileSync(file, '{broken', 'utf8');
    const s = createSettingsStore(file);
    await s.load();
    expect(s.getLang()).toBe('zh');
  });
});
```

- [ ] **Step 2: 确认失败**

Run: `cd server && npx vitest run tests/settings.test.ts`
Expected: FAIL(module not found)

- [ ] **Step 3: 写 settings.ts(复刻 rooms.ts 原子写模式)**

```ts
// 全局设置持久化(data/settings.json):当前仅 lang。
// 复刻 rooms.ts 的原子写模式:temp + rename + EPERM 重试;setLang 内嵌串行
// (单 key 无读改写竞争,写互斥从简——但仍走原子写防半截文件)。

import { readFile, writeFile, mkdir, rename } from 'node:fs/promises';
import { existsSync } from 'node:fs';
import path from 'node:path';
import { REPO_ROOT } from '../paths';
import type { Lang } from '../core/i18n/lang';

const DEFAULT_FILE = path.join(REPO_ROOT, 'data', 'settings.json');

export interface SettingsStore {
  load(): Promise<void>;
  getLang(): Lang;
  setLang(lang: Lang): Promise<void>;
}

export function createSettingsStore(file = DEFAULT_FILE): SettingsStore {
  let lang: Lang = 'zh';

  async function atomicWrite(content: string): Promise<void> {
    const tmp = file + '.tmp';
    await writeFile(tmp, content, 'utf8');
    try {
      await rename(tmp, file);
    } catch (e: any) {
      if (e?.code === 'EPERM') {
        await new Promise((r) => setTimeout(r, 50));
        await rename(tmp, file);
        return;
      }
      throw e;
    }
  }

  return {
    async load() {
      if (!existsSync(file)) return;
      try {
        const parsed = JSON.parse(await readFile(file, 'utf8'));
        if (parsed?.lang === 'en' || parsed?.lang === 'zh') lang = parsed.lang;
      } catch (e) {
        console.error('[settings-store] settings.json 损坏,回退默认:', e);
      }
    },
    getLang: () => lang,
    async setLang(next: Lang) {
      lang = next;
      await mkdir(path.dirname(file), { recursive: true });
      await atomicWrite(JSON.stringify({ lang: next }, null, 2));
    },
  };
}

/** 进程单例(index.ts 启动时 load;routes/core 注入点共用) */
export const settings = createSettingsStore();
```

- [ ] **Step 4: REST 端点 + ws hello**

`routes.ts`:顶部加

```ts
import { settings } from '../store/settings';
import { t } from '../core/i18n/messages';
```

在 `/api/adapters` 路由之前插入:

```ts
// ---- 全局设置(语言) ----
if (p === '/api/settings' && method === 'GET') {
  return json(res, 200, { lang: settings.getLang() });
}
if (p === '/api/settings/language' && method === 'PUT') {
  const body = await readBody(req);
  if (body.lang !== 'zh' && body.lang !== 'en') {
    return json(res, 400, { error: t(settings.getLang(), 'api.badLang') });
  }
  await settings.setLang(body.lang);
  return json(res, 200, { lang: body.lang });
}
```

`ws.ts`:`setupWs(server, bus)` 签名不动,hello 改:

```ts
import { settings } from '../store/settings';
import { t } from '../core/i18n/messages';
// ...
ws.send(JSON.stringify({ type: 'hello', payload: t(settings.getLang(), 'ws.hello') }));
```

- [ ] **Step 5: index.ts 装配**

`index.ts` main() 开头(`loadConfig` 之后):

```ts
import { settings } from '../store/settings';
// ...
await settings.load();
```

并把 `getLang: settings.getLang` 注入三处(本步先接线,消费在 Task 6/7):
1. `new ChatRoom(rcfg, bus, cfg.adapters, cfg.admin, roomPersistence, cfg.summary, { ...ports, getLang: settings.getLang })`
2. `new DirectChatService({ ..., getLang: settings.getLang })`(index.ts 目前不建 DirectChatService——它在 routes.ts:97 创建!所以这处注入在 routes.ts 做:`directChat` 的 deps 加 `getLang: settings.getLang`)
3. routes.ts 的 `adminInstance`(Task 7 改造 Admin 时消费)

- [ ] **Step 6: 跑测试 + 手工冒烟**

Run: `cd server && npm test && npm run typecheck`
然后 `npx tsx src/server/index.ts` 起服务,curl/python 验证:

```python
# python urllib(项目纪律:中文 JSON 不用 curl)
import urllib.request, json
print(json.load(urllib.request.urlopen('http://127.0.0.1:3220/api/settings')))  # {"lang":"zh"}
req = urllib.request.Request('http://127.0.0.1:3220/api/settings/language',
    data=json.dumps({'lang':'en'}).encode(), headers={'Content-Type':'application/json'}, method='PUT')
print(json.load(urllib.request.urlopen(req)))  # {"lang":"en"}
```

确认 `data/settings.json` 生成且内容 `{"lang":"en"}`;改回 zh 并删除该文件验证默认路径。

- [ ] **Step 7: Commit**

```bash
git add server/src/store/settings.ts server/src/server/routes.ts server/src/server/ws.ts server/src/server/index.ts server/tests/settings.test.ts
git commit -m "feat(i18n): settings 持久化 + GET/PUT /api/settings/language + ws hello 双语 + getLang 全链注入"
```

---

## Slice 3:后端消息词典替换(A 类)

### Task 6: orchestrator/engine/publish A 类消息 → t()

**Files:**
- Modify: `server/src/core/orchestrator.ts`(deps + getLang;:316-:812 全部 sysMessage/sysNotice/trigger)
- Modify: `server/src/core/modes/subscribe/engine.ts`(:427/:434/:451/:487/:528/:541)
- Modify: `server/src/core/modes/subscribe/publish.ts`(:87-88——deps 已有 sysMessage 通道,publish 措辞改 key 化需要 lang。**决策**:publish.ts 的 sysMessage 接缝改为接收**已渲染文本**(现状),调用方(engine/orchestrator)渲染;publish.ts 自身签名不变,由调用方在 `deps.sysMessage` 包一层渲染)——具体:engine.ts 构造 publishSpeechResult 的 deps 时 `sysMessage: (text) => this.deps.sysMessage(text)` 保持文本透传,但 silent 通知文本在 publish.ts 内拼——**改法**:publish.ts 增加 `deps.t?: (key: string, params?: Record<string, string|number>) => string` 可选接缝,engine/orchestrator 各自注入 `(key, params) => t(this.lang, key, params)`;publish 内 silent 通知改 `deps.t?.('sub.skipViolated', { name: speaker.name }) ?? '...(原中文串)'`——**不留双写**:直接 `deps.t!` 必传?不行,测试直调 publishSpeechResult。**定稿**:publish.ts 的 silent 通知改为由 `deps.sysMessage` 之外的**新可选钩子** `onSilent?: (kind: 'skip'|'violated', speaker: MemberConfig) => Promise<void>`,缺省时 publish 自己发原中文(现状);engine/orchestrator 注入该钩子渲染双语。**简化定稿**(最少侵入):silent 通知移出 publish,由调用方发——查消费方:engine.ts:425 已在调 publish 前自行判定 silent 并发通知后 return;publish 内的 silent 分支只服务 orchestrator.runOne 的订阅路径(orchestrator.ts:701 pubOutcome.wasSilent)。**最终方案**:publish.ts 保留 silent 分支但通知文本经 `deps.sysMessage(deps.renderSilent?.(mustRespond, speaker.name) ?? 原中文)`。不——过度工程。**采用**:publish.ts 的 SpeechPublishDeps 增加可选 `tSilent: (mustRespond: boolean, name: string) => string`,engine 与 orchestrator 注入;缺省回退原中文内联(现状,测试兼容)。
- Test: `server/tests/orchestrator.test.ts` **追加** en 场景(不改动既有断言)

**Interfaces:**
- Consumes: Task 4 `t/Lang/LangGetter`、Task 1 `batonTagFor`、Task 5 注入的 getLang
- Produces: `OrchestratorDeps.getLang?: LangGetter`(后续 room.ts 转发);`SubscribeEngineDeps.getLang?: LangGetter`;`SpeechPublishDeps.tSilent?: (mustRespond: boolean, name: string) => string`

- [ ] **Step 1: orchestrator.ts 注入与替换**

1. deps 接口加:

```ts
import { t } from './i18n/messages';
import { DEFAULT_LANG, type LangGetter } from './i18n/lang';
import { batonTagFor } from '../protocolKeywords';
// OrchestratorDeps 内:
  /** 语言注入(未注入 → zh,与改造前逐字节一致) */
  getLang?: LangGetter;
```

2. 类内:

```ts
  private get lang(): Lang { return this.deps.getLang?.() ?? DEFAULT_LANG; }
```

(`import type { Lang } from './i18n/lang';`)

3. 逐处替换(sysMessage/sysNotice → 渲染调用,行号为当前锚点):

| 行 | 原 | 改 |
|---|---|---|
| :298 | `` trigger: `${cmd.fromName} 指定你起头,…` `` | `trigger: t(this.lang, 'orch.trigger.designatedStart', { fromName: cmd.fromName })` |
| :316 | `已唤醒 ${names}…` | `t(this.lang, 'orch.wokeQueued', { names })` |
| :328/:342 | `没有找到 @ 的成员…` | `t(this.lang, 'orch.mentionNotFound')` |
| :335 | `已取消之前的指令…` | `t(this.lang, 'orch.mentionReplaced', { name: cmd.member.name })` |
| :338 | trigger(用户 @了你…) | `t(this.lang, 'orch.trigger.mentioned', { batonTag: batonTagFor(this.lang) })` |
| :366 | `讨论继续, 随机唤醒…` | `t(this.lang, 'orch.continueRandom', { name: starter.name })` |
| :394 | `讨论开始, 随机唤醒…` | `t(this.lang, 'orch.startRandom', { name: starter.name })` |
| :418-419 | starter reason | `t(this.lang, 'trace.pendingReason', { name })` / `t(this.lang, 'trace.coldStartPicked')`(此两值进 trigger 拼接,查上下文:它们拼进 `orch.trigger.starterReason` 的 reason 参数——核对后按原拼接结构传参) |
| :473 | `找不到重roll成员` | `t(this.lang, 'orch.rerollNotFound', { id: memberId })` |
| :500 | trigger(用户直接对你说) | `t(this.lang, 'orch.trigger.userDirect', { text: userText, batonTag: batonTagFor(this.lang) })` |
| :525 | `开始轮流发言 N 轮` | `t(this.lang, 'orch.roundsStart', { rounds })` |
| :631 | `'(已停止思考)'` | `t(this.lang, 'sys.stoppedThinking')` |
| :656 | `${member.name} 发言失败: …` | `t(this.lang, 'orch.speakFailed', { name: member.name, error: outcome.error ?? t(this.lang, 'sys.unknownError') })` |
| :671 | `'(无输出)'` | `t(this.lang, 'sys.noOutput')` |
| :721 | 自动发言上限 | `t(this.lang, 'orch.autoBudgetReached')` |
| :773/:778/:783/:789/:795/:800/:812 | 结束/交还/无下家/待命/上限/传递/轮流结束 | 对应 `orch.*` key |
| :803 | trigger(指定你接棒) | `t(this.lang, 'orch.trigger.batonToYou', { name: member.name })` |
| :940 | `适配器未配置: ${key}` | `t(this.lang, 'sys.adapterMissing', { key })` |
| :1015 | `|| '用户'` | `|| t(this.lang, 'sys.user')` |

另:凡 enqueu 的 trigger(:371/:398/:424/:433/:519-533 等未逐一列出的中文 trigger 字面量)全量扫一遍该文件,逐个对号入座 `orch.trigger.*` key;key 不够就**在 messages.ts 增补**(zh 逐字节拷贝 + en 翻译,同时更新 en 侧)。

4. **订阅引擎转发**:orchestrator.ts 构造 `new SubscribeEngine({...})` 的 deps(约 :120-135)加 `getLang: () => this.lang`。

- [ ] **Step 2: engine.ts 替换**

deps 加 `getLang?: LangGetter`,类内 `private get lang()` 同款;:427 `sub.silentSkip`、:434 `orch.autoBudgetReached`(与 orchestrator 同 key——订阅与接棒共用同一句中文,en 亦同文)、:451/:487 `trace.heartbeat`、:528 `sub.dmMention`、:541 `sub.atMention`;构造 publishSpeechResult deps 时注入:

```ts
tSilent: (mustRespond, name) =>
  t(this.lang, mustRespond ? 'sub.skipViolated' : 'sub.silentSkip', { name }),
```

- [ ] **Step 3: publish.ts tSilent 接缝**

`SpeechPublishDeps` 加:

```ts
  /** silent 通知渲染(双语);缺省回退原中文(测试直调兼容) */
  tSilent?: (mustRespond: boolean, name: string) => string;
```

:85-89 改:

```ts
  if (isSilentDecision(rawText)) {
    const text = deps.tSilent
      ? deps.tSilent(mustRespond, speaker.name)
      : mustRespond
        ? `${speaker.name} 尝试跳过发言(点名场景不允许跳过),本次发言已忽略。`
        : `${speaker.name} 评估暂无发言与私聊意向,选择跳过。`;
    await deps.sysMessage(text);
    return { wasSilent: true, publishedIds: [], bubbles: [] };
  }
```

- [ ] **Step 4: 追加 en 场景测试(orchestrator.test.ts 文件末尾)**

```ts
import { t } from '../src/core/i18n/messages';

describe('i18n:en 语言注入', () => {
  it('getLang=en → 系统消息与 trigger 英文;缺省 zh 逐字节不变', async () => {
    const h = makeHarness([{ result: '第一棒\n<pass>@乙' }]);
    // en harness:makeHarness 加第 5 参?不改既有签名——手工再建一个 deps 注入 getLang
    const mem = makeMembers(3, ['甲', '乙', '丙']);
    const room = makeRoomConfig(mem);
    const fake = makeFakeAdapter([{ result: 'done\n<pass>end' }]);
    const messages: ChatMessage[] = [];
    const orch = new Orchestrator({
      room,
      adapterConfigs: { fake: { command: 'fake', args: [] } },
      resolveAdapter: () => fake.impl,
      pushMessage: async (m) => { messages.push(m); },
      sysMessage: async (text) => { messages.push({ id: `s${messages.length}`, roomId: room.id, from: 'system', fromName: 'System', text, ts: Date.now(), system: true }); },
      onStatuses: () => {},
      persistRoom: async () => {},
      runScout: async () => null,
      getHistory: () => messages,
      pushAgentEvent: () => {},
      getLang: () => 'en',
    });
    await orch.onUserMessage('start');
    await settle(200);
    const sys = messages.filter((m) => m.system).map((m) => m.text).join('|');
    expect(sys).toContain('Discussion starts');
    expect(sys).toContain('declared the discussion closed'); // <pass>end → en 结束消息
    expect(sys).not.toContain('宣布讨论结束');
    // zh 路径回归:既有 harness 不注入 getLang
    const h2 = makeHarness([{ result: '总结\n<接棒>结束' }]);
    await h2.orch.onUserMessage('开始');
    await settle(150);
    const sys2 = h2.messages.filter((m) => m.system).map((m) => m.text).join('|');
    expect(sys2).toContain('宣布讨论结束'); // 中文原样
  });
});
```

- [ ] **Step 5: 跑全量测试**

Run: `cd server && npm test && npm run typecheck`
Expected: 全绿。subscribe.test 若断言 silent 通知中文——engine 注入 tSilent 后 en 注入才变;测试未注入 getLang → zh 原文 ✓。

- [ ] **Step 6: Commit**

```bash
git add server/src/core/orchestrator.ts server/src/core/modes/subscribe/engine.ts server/src/core/modes/subscribe/publish.ts server/tests/orchestrator.test.ts
git commit -m "feat(i18n): orchestrator/engine/publish A 类消息走词典渲染(getLang 注入,缺省 zh 不变)"
```

### Task 7: room/direct/admin/routes/historyOps/sessionGuard A 类 → t()

**Files:**
- Modify: `server/src/core/room.ts`(:120/:150-157/:234-239/:308/:318/:321/:420-422/:464/:481/:490/:497-503/:610-612)
- Modify: `server/src/core/direct.ts`(:182/:204-210/:297/:321/:354/:364/:411-435 的 A 类部分/:451/:464)
- Modify: `server/src/core/admin.ts`(:83/:88/:119-123/:163-192 的 A 类部分——摘要 prompt 是 B 类留给 Task 9)
- Modify: `server/src/core/historyOps.ts`(:9/:21/:24)
- Modify: `server/src/core/sessionGuard.ts`(:27)
- Modify: `server/src/server/routes.ts`(~30 处 error + :114/:134/:140/:143/:146/:157/:177/:688/:704/:725/:741/:757)
- Test: 既有测试全绿即可(rooms-store/direct-chat/user-persona 等断言中文,zh 路径不变)

**Interfaces:**
- Consumes: Task 4 `t`、Task 5 `settings.getLang`、`LangGetter`
- Produces: `RoomPersistence` 不变;ChatRoom 构造第 7 参 ports 增 `getLang?: LangGetter`;`DirectChatServiceDeps.getLang?: LangGetter`;`AdminConfig` 不变,Admin 构造第 4 参 `getLang?: LangGetter`

- [ ] **Step 1: room.ts**

1. 构造 ports 类型加 `getLang?: LangGetter`,存 `private getLangFn`;类内 `private get lang(): Lang`。
2. 替换::464 `fromName: '系统'` → `t(this.lang, 'sys.speaker')`;:481 `'用户'` → `t(this.lang, 'sys.user')`;:308/:321 成员进退;:318/:490 `成员不存在`;:497/:501/:503 start 提示;:610-612 `makeRoomConfig` 默认值——**makeRoomConfig 是纯工厂函数**(非实例方法),加参 `makeRoomConfig(input, lang: Lang = 'zh')`:`name: input.name || t(lang, 'room.defaultName')`、`topic: input.topic || t(lang, 'room.defaultTopic')`。routes.ts 两处调用 `makeRoomConfig(...)`(建房/导入)传 `settings.getLang()`。:120 admin adapter 错误 → `t(this.lang, 'room.adminAdapterMissing', { key })`;:150-157/:234-239/:420-422 trace 标签 → 对应 `trace.*` key;:464 '系统' memberName(runScout traceLog 内 :150)同。
3. Orchestrator deps 构造处(:131-174)加 `getLang: () => this.lang`。

- [ ] **Step 2: direct.ts**

deps 加 `getLang?: LangGetter` + `private get lang()`;:182 `'用户'`、:209/:353/:464 `'系统'`、:204 适配器未配置、:297 trigger `'1v1用户对话'` → `trace.directChat`、:321/:364 占位、:354 回复失败、:451 摘要 topic `admin.directTopic`。routes.ts 构造 DirectChatService 处注入 `getLang: settings.getLang`(Task 5 已留位)。

- [ ] **Step 3: admin.ts**

构造函数加第 4 参 `private getLang: LangGetter = () => 'zh'`,`private get lang()`;:83 `'🔍 侦察员'` → `t(this.lang, 'sys.scout')`;:88 trigger;:119/:123 熔断文案。routes.ts 的 `new Admin(...)`(routes.ts:86-95)加第 4 参 `settings.getLang`;room.ts 内 `new Admin(...)`(:122-130)同样。

- [ ] **Step 4: historyOps/sessionGuard**

抛错改 `throw new Error(t(lang, 'ho.msgNotFound', { id: messageId }))`——这两个是**纯函数模块**,无注入点;调用链:routes.ts catch 后 `String(e)` 透传。**最小改法**:函数签名加 `lang: Lang = 'zh'` 尾参,routes.ts 调用点传 `settings.getLang()`。核对 routes.ts 对 truncate/reroll/edit 的调用点(约 :590-:640/:600-:630 两处 room/direct)逐一补参。sessionGuard 同款(`assertSessionConfigurable(..., lang)`)。

- [ ] **Step 5: routes.ts error 全量替换**

顶部已有 `import { t } from '../core/i18n/messages'`(Task 5 加过)。定义局部 `const L = () => settings.getLang();` 然后**全部 30 处** `error: '...'` → `error: t(L(), 'api.xxx')`、`` error: `...${x}` `` → `error: t(L(), 'api.xxx', { key: x })`(对照 Task 4 Step 4 清单;参数名以词典定义为准)。同法::114/:134/:140/:143(doImport 内 throw)、:146 `未命名房间` → `t(L(), 'api.unnamedRoom')`、:157 `成员${idx+1}` → `api.memberN`、:177 `'用户'` → `'sys.user'`、:704 `'成员'` → `api.member`、:688/:725 systemNotice → `api.instructionFailed/orchStartFailed`(msg 参数传 `String(e)`)、:741、:757。index.ts:91 静态兜底文案 `前端未构建…` 保留中文(仅开发者可见,C 类)。

- [ ] **Step 6: 全量测试 + typecheck**

Run: `cd server && npm test && npm run typecheck`
Expected: 全绿。direct-chat.test/user-persona.test 断言中文 → 未注入 getLang → zh 原文 ✓。

- [ ] **Step 7: Commit**

```bash
git add server/src/core/room.ts server/src/core/direct.ts server/src/core/admin.ts server/src/core/historyOps.ts server/src/core/sessionGuard.ts server/src/server/routes.ts
git commit -m "feat(i18n): room/direct/admin/routes/historyOps/sessionGuard A 类消息走词典(缺省 zh 不变)"
```

---

## Slice 4:后端 prompt 词典(B 类)

### Task 8: promptTexts.ts + render/prompt/baton/subscribe prompt 双语

**Files:**
- Create: `server/src/core/i18n/promptTexts.ts`
- Modify: `server/src/core/render.ts`(historyText/lengthBrief/permissionBrief 加 lang 尾参)
- Modify: `server/src/core/prompt.ts`(buildPrompt/buildDeltaPrompt/buildScoutPrompt 加 lang)
- Modify: `server/src/core/modes/baton/prompt.ts`(buildBatonPromptSection 加 lang;标签用 batonTagFor)
- Modify: `server/src/core/modes/subscribe/prompt.ts`(buildHeartbeatPrompt/buildSubscribePromptSection 加 lang)
- Modify: `server/src/core/orchestrator.ts`(4 处 buildPrompt/buildDeltaPrompt 调用传 this.lang)、`engine.ts`(buildHeartbeatPrompt 传 lang)
- Test: `server/tests/prompt-i18n.test.ts`(新建);既有 baton/subscribe/context-mode 断言不动

**Interfaces:**
- Produces: `PROMPT_TEXTS: { zh: PromptTextDict; en: PromptTextDict }`(key → `(p?: any) => string`);各 prompt 组装器签名尾部 `lang: Lang = 'zh'`(默认参数保既有调用)
- 消费纪律:纯函数显式 lang 参数;标签一律 `batonTagFor(lang)`/`dmTagFor(lang)`/`handshakeTagFor(kind, lang)`,**严禁手写标签字面量**

- [ ] **Step 1: 建 promptTexts.ts 结构 + zh 侧(逐字节拷贝)+ en 侧**

key 清单(zh 侧源码锚点;en 侧完整翻译——核心段落全文如下,其余标题类短句按同风格):

```ts
// B 类 prompt 段落词典(zh/en)。zh = 现状源文逐字节拷贝(测试断言的锁);
// en 翻译。组装逻辑(prompt.ts 的顺序/拼接/分支)不动——本文件只放"句子"。
import type { Lang } from './lang';
import { batonTagFor, dmTagFor, handshakeTagFor } from '../../protocolKeywords';

type P = Record<string, string | number>;
type PromptTextDict = Record<string, (p?: P) => string>;

export const PROMPT_TEXTS: { zh: PromptTextDict; en: PromptTextDict } = {
  zh: {
    // render.ts:21-37 转录标签
    'r.privateFromYou': (p) => `> 🔒 **【私聊密信 ── 你 发送给对方】：**\n> ${p?.text ?? ''}`,
    'r.privateToYou': (p) => `> 🔒 **【私聊密信 ── ${p?.name ?? ''} 对 你 悄悄说】：**\n> ${p?.text ?? ''}`,
    'r.privateOther': (p) => `> 🔒 **【私聊密信 ── 来自 ${p?.name ?? ''}】：**\n> ${p?.text ?? ''}`,
    'r.mentioned': (p) => `**🎯【@提及了你】[${p?.name ?? ''}] (全员公聊)：**\n${p?.text ?? ''}`,
    'r.publicMsg': (p) => `**[${p?.name ?? ''}] (全员公聊)：**\n${p?.text ?? ''}`,
    // render.ts:46/48/51
    'r.lengthLong': () => '发言长度不限,把论证、证据、推理过程充分展开,像给同事写一份严肃的技术论述。',
    'r.lengthNormal': () => '发言控制在 600 字以内,论证完整、有理有据,不空泛。',
    'r.lengthShort': () => '发言控制在 300 字以内,观点鲜明。如果你不认同某人的说法,直接指出并说明理由。',
    // render.ts:59/62/65
    'r.permReadwrite': () => '你可以使用 Read / Grep / Glob 工具阅读项目,也可以用 Edit / Write 修改项目文件(谨慎,仅在讨论确有必要时)。',
    'r.permFull': () => '你可以使用全部工具(读写项目文件、执行命令)围绕项目工作。',
    'r.permReadonly': () => '你可以使用 Read / Grep / Glob 工具阅读和搜索项目文件,但不能修改任何文件。',

    // prompt.ts:42-51 段落头
    'p.roleHeader': (p) => `# 你的角色\n你是 **【${p?.name ?? ''}】**。\n${p?.persona ?? ''}`,
    'p.roomHeader': (p) => `# 房间:${p?.name ?? ''}\n讨论主题:${p?.topic ?? ''}`,
    'p.othersHeader': (p) => `# 其他参与者\n${p?.list ?? ''}`,
    'p.ownerSection': (p) => `# 对话者/房主设定\n当前在房间中与你们交流探讨的用户身份为 **【${p?.name ?? ''}】**。\n其背景设定与立场如下:\n${p?.persona ?? ''}`,
    // prompt.ts:60-75 项目段
    'p.projectScoutSection': (p) => `# 讨论对象:本地项目(侦察报告已有,无需重复探索)\n项目根目录:\`${p?.root ?? ''}\`\n\n下方是侦察员已完成的项目分析报告,直接基于它讨论即可——\n**不要重复用工具浏览项目**,只有报告未覆盖且讨论确需某个具体文件细节时才单独去读。\n\n---\n${p?.report ?? ''}\n---`,
    'p.projectFreshSection': (p) => `# 讨论对象:本地项目\n本次讨论围绕一个真实项目进行。项目根目录:\`${p?.root ?? ''}\`\n\n目录结构概览:\n\`\`\`\n${p?.tree ?? ''}\n\`\`\`\n\n${p?.perm ?? ''}\n需要深入了解某个文件时,直接用工具去读,不要凭目录名猜测内容。首次发言前建议先浏览关键文件再表态。`,
    // prompt.ts:97-105 历史段标签
    'p.publicSummaryLabel': () => '【前期讨论摘要】',
    'p.privateDigestLabel': () => '【你的私聊往来纪要(仅你可见)】',
    'p.recentHistoryLabel': () => '【近期讨论发言(按时间顺序,最新在后)】',
    'p.historySectionHeader': () => '# 聊天记录与讨论背景',
    // prompt.ts:113
    'p.speakDirectly': () => '请直接以你的角色身份发言。不要复述设定,不要使用 markdown 标题,直接说出你的观点/回应。',
    'p.nowTurn': (p) => `# 现在轮到你发言\n${p?.instructions ?? ''}`,
    'p.topicFallback': () => '自由讨论',

    // prompt.ts:171-194 delta 段
    'd.identityAnchor': (p) => `你是 **【${p?.name ?? ''}】**。请始终保持你的 **既有人设与核心立场**。`,
    'd.unreadHeader': () => '# 自你上次发言以来的最新未读动态:',
    'd.noNewMsg': () => '# 自你上次发言以来暂无新增动态。',
    'd.taskHeader': (p) => `# 你的行动任务:\n${p?.brief ?? ''}`,
    'd.extraInstruction': (p) => `# 额外指令:\n${p?.text ?? ''}`,

    // prompt.ts:200-217 侦察(整段一组 key,实现时按行拆或整段一个 key 均可——定稿:整段单 key 's.scoutPrompt')
    's.scoutPrompt': (p) => `你是项目侦察员。快速分析下面的项目并输出一份精炼的《项目侦察报告》,供一组 AI 讨论者直接使用(他们不会再重复读文件)。\n\n报告结构(纯文本,总共 400 字以内):\n1. 项目是什么(一句话)\n2. 技术栈与关键依赖\n3. 目录结构与核心模块(各一行说明)\n4. 数据存储现状(如有:现有数据库/文件存储/无)\n5. 值得讨论者注意的 2-3 个特点/约束\n\n项目根目录:${p?.root ?? ''}\n目录概览:\n${p?.tree ?? ''}\n\n你可以使用 Read/Glob/Grep 工具查看必要文件(README、配置、入口代码),但控制在 10 次以内,快速完成。直接输出报告正文,不要客套。`,

    // baton/prompt.ts(标签经 batonTagFor 注入参数)
    'b.chainRule': () => '这是接棒链讨论,你发完言后由你决定下一位发言者,TA 会立即接着发言。',
    'b.calloutRule': () => '你在回应用户的点名。回应完毕后,由你指定下一位发言者——讨论将暂停,等用户发话后 TA 才开始。',
    'b.section': (p) => `# 接棒规则 (重要)\n${p?.rule ?? ''}\n发言正文结束后,另起一行写接棒指令(与用户输入语法一致):\n- 想让谁接话:最后一行写 **${p?.tag ?? '<接棒>'}@名字** (从: ${p?.others ?? ''} 中选)\n- 认为讨论已充分收敛、没有继续的必要:最后一行写 **${p?.tag ?? '<接棒>'}结束**\n选择依据:谁的观点被你质疑了、谁还没说过话、谁的视角最适合回应你刚才的内容。**严禁接棒给自己**。`,

    // subscribe/prompt.ts 心跳(55-115 大段;tag 经参数)
    'h.identity': (p) => `# 你的身份\n你是 **【${p?.name ?? ''}】**。\n立场/人设: ${p?.persona ?? ''}`,
    'h.whitelistHeader': () => '# 可互动同事白名单 (本名单已排除你自己;**严禁给自己发私聊**;**严禁脑补数字后缀如 @名字1**)',
    'h.topic': (p) => `# 讨论主题: ${p?.topic ?? ''}`,
    'h.summaryLabel': () => '# 前期讨论摘要',
    'h.digestLabel': () => '# 你的私聊往来纪要 (仅你可见)',
    'h.unreadHeader': () => '# 自上次查看以来的最新未读消息',
    'h.noNewMsg': () => '(自上次查看以来暂无新消息)',
    'h.twoPhase': (p) => `# 你的行动决策 (两阶段自决法则)\n阅读上述最新讨论，你有完全的自主决定权。请务必按以下两阶段推进你的思考：\n\n【第一阶段：意向自评 (先评估意图，再构思内容)】\n请在心中自评两件事：\n1. **公聊意愿 (0 ~ 100 分)**：你此时在大群公开发言的迫切度。\n   - < 60分：观点刚才已表达清楚、或话题与你关系不大、或想先看别人怎么吵、保持沉默更有利。\n   - ≥ 60分：被他人直接质问、面临重大危机、或有不可不发的新立场/新反驳必须公开宣布。\n2. **私聊意向 (有 / 无)**：审视上方同事白名单，你此刻是否想私下给某人单独通个气、对个暗号、商量对策、提醒兄弟、或暗中结盟？\n   - 若有，明确私聊对象是谁，核心想私下沟通什么 (支持单人如 @名字，也支持同时找多个人如 @名字A @名字B)。\n\n【第二阶段：按自评结果执行输出】\n- 若【公聊意愿 < 60 且 无私聊意向】：**严格直接输出 <跳过>** (保持潜水观望局势，绝不多言)。\n- 若【公聊意愿 ≥ 60 且 无私聊意向】：**直接输出公开发言正文** (纯公聊，无需附带私聊)。\n- 若【公聊意愿 < 60 但 有私聊意向】：**直接输出: <私聊>@同事名字 私信内容** (纯私聊，不发大群公聊)。\n- 若【公聊意愿 ≥ 60 且 有私聊意向】：**先写公开发言，并在结尾另起一行附带: <私聊>@同事名字 私信内容** (系统会自动拆分为公聊与私聊两个独立气泡)。`,
    'h.dmChannel': (p) => `# 当前私聊通道\n你与 **${p?.name ?? ''}** 此前开启过私聊 (第 **${p?.count ?? 1}/3** 轮)。\n若你认为私聊话题已达成共识或已破裂，可直接发公聊；若仍需在私信中回复对方，可按需在私聊末尾附带态度标签:\n- **<同意>** (认同对方意见，终结私聊)\n- **<拒绝>** (拒绝对方方案，终结私聊)\n- **<想法> @${p?.name ?? ''}** (提出补充条件继续私聊)`,
    'h.noBatonRule': () => `【重要规则】**禁止输出 <接棒> 标签**。若公聊意愿低于60分且无私聊意向，**严格仅输出 <跳过> 四个字符**，不要输出多余废话。`,

    // subscribe/prompt.ts:155-174 通用段
    'sb.mustRespond': (p) => `# 讨论模式说明(订阅模式 · 你被直接点名)\n用户或同事直接点名要你回应——这是强制发言,你没有跳过权。\n**禁止输出 <跳过> 或 <沉默>**,必须就当前话题做出实质性回应(观点/反驳/补充均可)。\n若想私下沟通,可在发言中附带: <私聊>@名字 悄悄话内容 (系统会自动拆分为独立气泡发布)。\n不要在文末输出 <接棒> 标签。\n可互动同事白名单(严禁给自己发私聊,严禁脑补数字后缀):\n\`\`\`json\n${p?.whitelist ?? ''}\n\`\`\``,
    'sb.auto': (p) => `# 讨论模式说明(订阅模式 · 自主在线群聊)\n本次讨论由心跳自主驱动，无需指定接棒人，不要在文末输出 <接棒> 标签。\n【两阶段自决】发言前请先自省：1. 公聊意愿是否≥60分？ 2. 是否有必要私下找人通气？\n- 若公聊意愿不足60分且无需私聊，直接输出 <跳过> 潜水观望；\n- 若需公开发言，直接阐述你的观点；\n- 若想私下找某位或多位同事单聊/通气/结盟，可在发言中(或独立)附带: <私聊>@名字 悄悄话内容 (系统会自动将公开发言与私信拆分为独立气泡发布)；\n可互动同事白名单(严禁给自己发私聊，严禁脑补数字后缀):\n\`\`\`json\n${p?.whitelist ?? ''}\n\`\`\``,
  },
  en: {
    'r.privateFromYou': (p) => `> 🔒 **[Private DM — you sent to the other side]:**\n> ${p?.text ?? ''}`,
    'r.privateToYou': (p) => `> 🔒 **[Private DM — ${p?.name ?? ''} whispers to you]:**\n> ${p?.text ?? ''}`,
    'r.privateOther': (p) => `> 🔒 **[Private DM — from ${p?.name ?? ''}]:**\n> ${p?.text ?? ''}`,
    'r.mentioned': (p) => `**🎯[@mentioned you] [${p?.name ?? ''}] (public to all):**\n${p?.text ?? ''}`,
    'r.publicMsg': (p) => `**[${p?.name ?? ''}] (public to all):**\n${p?.text ?? ''}`,
    'r.lengthLong': () => 'No length limit. Fully develop your arguments, evidence and reasoning, like a serious technical essay for colleagues.',
    'r.lengthNormal': () => 'Keep each speech under 600 words: complete reasoning, well-grounded, not vague.',
    'r.lengthShort': () => 'Keep each speech under 300 words with a sharp position. If you disagree with someone, say so directly and explain why.',
    'r.permReadwrite': () => 'You may use Read / Grep / Glob tools to read the project, and Edit / Write to modify project files (cautiously, only when the discussion truly requires it).',
    'r.permFull': () => 'You may use all tools (read/write project files, run commands) to work on the project.',
    'r.permReadonly': () => 'You may use Read / Grep / Glob tools to read and search project files, but must not modify any file.',

    'p.roleHeader': (p) => `# Your Role\nYou are **[${p?.name ?? ''}]**.\n${p?.persona ?? ''}`,
    'p.roomHeader': (p) => `# Room: ${p?.name ?? ''}\nDiscussion topic: ${p?.topic ?? ''}`,
    'p.othersHeader': (p) => `# Other Participants\n${p?.list ?? ''}`,
    'p.ownerSection': (p) => `# The User / Room Owner\nThe user talking with you in this room is **[${p?.name ?? ''}]**.\nTheir background and stance:\n${p?.persona ?? ''}`,
    'p.projectScoutSection': (p) => `# Subject: local project (scout report already exists — do not re-explore)\nProject root: \`${p?.root ?? ''}\`\n\nBelow is the scout's project analysis report. Discuss based on it directly —\n**do NOT browse the project with tools again**; only read a specific file if the report misses it and the discussion truly needs the detail.\n\n---\n${p?.report ?? ''}\n---`,
    'p.projectFreshSection': (p) => `# Subject: local project\nThis discussion revolves around a real project. Project root: \`${p?.root ?? ''}\`\n\nDirectory overview:\n\`\`\`\n${p?.tree ?? ''}\n\`\`\`\n\n${p?.perm ?? ''}\nWhen you need to understand a file, read it with tools directly instead of guessing from directory names. Before your first speech, skim key files before taking a stance.`,
    'p.publicSummaryLabel': () => '[Earlier discussion summary]',
    'p.privateDigestLabel': () => '[Your private DM history digest (visible only to you)]',
    'p.recentHistoryLabel': () => '[Recent speeches (chronological, newest last)]',
    'p.historySectionHeader': () => '# Chat History & Context',
    'p.speakDirectly': () => 'Speak directly in character. Do not restate your setup, do not use markdown headings — just state your view/response.',
    'p.nowTurn': (p) => `# It Is Your Turn to Speak\n${p?.instructions ?? ''}`,
    'p.topicFallback': () => 'free discussion',

    'd.identityAnchor': (p) => `You are **[${p?.name ?? ''}]**. Always keep your **established persona and core stance**.`,
    'd.unreadHeader': () => '# New messages since your last speech:',
    'd.noNewMsg': () => '# No new activity since your last speech.',
    'd.taskHeader': (p) => `# Your Task:\n${p?.brief ?? ''}`,
    'd.extraInstruction': (p) => `# Extra Instruction:\n${p?.text ?? ''}`,

    's.scoutPrompt': (p) => `You are a project scout. Quickly analyze the project below and produce a concise "Project Scout Report" for a group of AI discussants to use directly (they will not re-read files).\n\nReport structure (plain text, within 400 words total):\n1. What the project is (one sentence)\n2. Tech stack and key dependencies\n3. Directory structure and core modules (one line each)\n4. Data storage status (if any: existing DB / file storage / none)\n5. 2-3 notable traits/constraints for discussants\n\nProject root: ${p?.root ?? ''}\nDirectory overview:\n${p?.tree ?? ''}\n\nYou may use Read/Glob/Grep tools to inspect necessary files (README, configs, entry code), but keep it within 10 calls and finish fast. Output the report body directly, no pleasantries.`,

    'b.chainRule': () => 'This is a baton-chain discussion. After you finish speaking, you decide the next speaker — they will speak immediately after you.',
    'b.calloutRule': () => 'You are responding to the user\'s call-out. After responding, you pick the next speaker — the discussion pauses, and they only start after the user sends another message.',
    'b.section': (p) => `# Baton Rules (Important)\n${p?.rule ?? ''}\nAfter your speech body, start a new line with the baton directive (same syntax the user uses):\n- To pass to someone: on the last line write **${p?.tag ?? '<pass>'}@name** (choose from: ${p?.others ?? ''})\n- If the discussion has sufficiently converged and need not continue: on the last line write **${p?.tag ?? '<pass>'}end**\nHow to choose: whose view did you challenge, who hasn't spoken yet, whose perspective best responds to what you just said. **Never pass the baton to yourself**.`,

    'h.identity': (p) => `# Your Identity\nYou are **[${p?.name ?? ''}]**.\nStance/persona: ${p?.persona ?? ''}`,
    'h.whitelistHeader': () => '# Interactable Colleague Whitelist (yourself excluded; **never DM yourself**; **never invent numeric suffixes like @name1**)',
    'h.topic': (p) => `# Discussion Topic: ${p?.topic ?? ''}`,
    'h.summaryLabel': () => '# Earlier Discussion Summary',
    'h.digestLabel': () => '# Your Private DM Digest (visible only to you)',
    'h.unreadHeader': () => '# Unread Messages Since You Last Checked',
    'h.noNewMsg': () => '(no new messages since you last checked)',
    'h.twoPhase': (p) => `# Your Action Decision (Two-Phase Self-Determination)\nRead the latest discussion above. You have full autonomy. Proceed in two phases:\n\n[Phase 1: Intent Self-Assessment (assess intent before composing)]\nPrivately evaluate two things:\n1. **Public-speech willingness (0–100)**: how urgently you want to speak publicly in the group right now.\n   - < 60: your view was already made clear, the topic has little to do with you, you want to watch the argument unfold first, or staying silent is safer.\n   - ≥ 60: you were directly challenged, face a major crisis, or have a new position/rebuttal that must be declared publicly.\n2. **DM intent (yes/no)**: review the colleague whitelist above — do you want to privately message someone right now: align, exchange signals, scheme, warn an ally, or form a covert alliance?\n   - If yes, decide who, and what you want to communicate privately (single target like @name, or multiple like @nameA @nameB).\n\n[Phase 2: Execute According to Your Self-Assessment]\n- If [public willingness < 60 AND no DM intent]: **output exactly ${p?.silentTag ?? '<skip>'}** (stay lurking; say nothing more).\n- If [public willingness ≥ 60 AND no DM intent]: **output your public speech directly** (pure public, no DM attached).\n- If [public willingness < 60 BUT DM intent]: **output exactly: ${p?.dmTag ?? '<dm>'}@colleague private message** (pure DM, no group speech).\n- If [public willingness ≥ 60 AND DM intent]: **write your public speech first, then append on a new line at the end: ${p?.dmTag ?? '<dm>'}@colleague private message** (the system splits it into a public bubble and a private bubble).`,
    'h.dmChannel': (p) => `# Active DM Channel\nYou previously opened a DM with **${p?.name ?? ''}** (round **${p?.count ?? 1}/3**).\nIf you believe the topic has reached consensus or broken down, you may simply speak publicly; if you still need to reply in the DM, you may append a stance tag at the end:\n- **${p?.agreeTag ?? '<agree>'}** (endorse the other side, closing the DM)\n- **${p?.rejectTag ?? '<decline>'}** (reject the proposal, closing the DM)\n- **${p?.ideaTag ?? '<idea>'} @${p?.name ?? ''}** (raise a supplementary condition to continue the DM)`,
    'h.noBatonRule': (p) => `【Important】**Do NOT output the ${p?.batonTag ?? '<pass>'} tag.** If public willingness is below 60 and there is no DM intent, **output exactly ${p?.silentTag ?? '<skip>'} and nothing else** — no extra words.`,

    'sb.mustRespond': (p) => `# Discussion Mode (Subscribe · You Were Directly Called Out)\nThe user or a colleague directly called you out — this is a mandatory speech; you have no right to skip.\n**Do NOT output ${p?.silentTag ?? '<skip>'} or <沉默>**, you must respond substantively to the topic (view/rebuttal/supplement all fine).\nTo communicate privately, append to your speech: ${p?.dmTag ?? '<dm>'}@name secret message (the system splits it into a separate bubble).\nDo not output a ${p?.batonTag ?? '<pass>'} tag at the end.\nInteractable colleague whitelist (never DM yourself, never invent numeric suffixes):\n\`\`\`json\n${p?.whitelist ?? ''}\n\`\`\``,
    'sb.auto': (p) => `# Discussion Mode (Subscribe · Autonomous Group Chat)\nThis discussion is heartbeat-driven; no baton-passing needed — do not output a ${p?.batonTag ?? '<pass>'} tag at the end.\n[Two-phase self-determination] Before speaking, ask yourself: 1. Is public willingness ≥ 60? 2. Is a private DM necessary?\n- If below 60 and no DM needed, output ${p?.silentTag ?? '<skip>'} and lurk;\n- If public speech is due, state your view directly;\n- To privately reach one or more colleagues (align/scheme/ally), append to your speech (or standalone): ${p?.dmTag ?? '<dm>'}@name secret message (the system splits public speech and DM into separate bubbles);\nInteractable colleague whitelist (never DM yourself, never invent numeric suffixes):\n\`\`\`json\n${p?.whitelist ?? ''}\n\`\`\``,
  },
};

/** 渲染一个 prompt 段落:lang → zh 兜底 */
export function pt(lang: Lang, key: string, params?: Record<string, string | number>): string {
  const fn = PROMPT_TEXTS[lang]?.[key] ?? PROMPT_TEXTS.zh[key];
  if (!fn) return key;
  return fn(params);
}
export { batonTagFor, dmTagFor, handshakeTagFor };
```

⚠️ **拷贝纪律与标签纪律**:zh 侧 `'h.twoPhase'/'h.dmChannel'/'h.noBatonRule'/'sb.*'` 内嵌的 `<跳过>/<私聊>/<同意>/<拒绝>/<想法>/<接棒>/<沉默>` 字面量——zh 侧**保留原字面量**(与现状逐字节一致);en 侧全部经参数注入(`silentTag/dmTag/agreeTag/rejectTag/ideaTag/batonTag`,调用点传 `dmTagFor(lang)` 等)。`sb.mustRespond` en 侧有一个 `<沉默>` 残留——改为 `${p?.silentTag2 ?? '<silent>'}`,调用点补传。实现时对 subscribe/prompt.ts 全文逐行核对 zh 拷贝。whitelist JSON 的 key:zh `{ 可互动同事白名单: names }`、en `{ colleagues: names }`——该 JSON 组装在 prompt.ts 组装器内做(按 lang 选 key),不放词典。

- [ ] **Step 2: 组装器加 lang 参数并替换文案引用**

统一模式(以 buildBatonPromptSection 为例):

```ts
import { pt } from '../../i18n/promptTexts';
import { batonTagFor } from '../../../../protocolKeywords';
import type { Lang } from '../../i18n/lang';

export function buildBatonPromptSection(
  member: MemberConfig,
  members: MemberConfig[],
  batonMode: 'chain' | 'callout',
  lang: Lang = 'zh',
): string {
  const otherNames = members.filter((m) => m.id !== member.id).map((m) => m.name);
  const rule = pt(lang, batonMode === 'chain' ? 'b.chainRule' : 'b.calloutRule');
  return pt(lang, 'b.section', {
    rule,
    tag: batonTagFor(lang),
    others: otherNames.join(' / '),
  });
}
```

同法改:
- `render.ts`:`historyText(messages, recent, viewerId, viewerName, lang: Lang = 'zh')`——5 个标签走 `pt(lang, 'r.*', { name, text })`;`lengthBrief(style, lang='zh')`;`permissionBrief(p, lang='zh')`。**historyText 的调用点**(prompt.ts:103/:175、direct.ts:237/:432、subscribe/prompt.ts:49)由各自组装器传 lang。
- `prompt.ts`:`buildPrompt(..., opts, lang)`?**定稿**:lang 放 opts 内 `opts.lang?: Lang`(默认 zh)——调用点 orchestrator.ts:557/:566/:575/:918 传 `{ ..., lang: this.lang }`;`buildDeltaPrompt` 同;`buildScoutPrompt(projectRoot, tree, lang='zh')`。内部各 parts.push 的中文字面量全部换 `pt(lang, 'p.*'/'d.*'/'s.*', {...})`;`room.topic || '自由讨论'` → `room.topic || pt(lang, 'p.topicFallback')`。
- `subscribe/prompt.ts`:`buildHeartbeatPrompt(..., lang: Lang = 'zh')` 尾参(engine.ts:408 调用传 `this.lang`);`buildSubscribePromptSection(member, members, opts, lang: Lang = 'zh')` 尾参(prompt.ts 两处调用传 opts.lang)。whitelist JSON key 按 lang:`JSON.stringify(lang === 'en' ? { colleagues: candidateNames } : { 可互动同事白名单: candidateNames }, null, 2)`。

- [ ] **Step 3: 写 prompt-i18n 测试**

```ts
// server/tests/prompt-i18n.test.ts
import { describe, it, expect } from 'vitest';
import { buildBatonPromptSection } from '../src/core/modes/baton/prompt';
import { buildSubscribePromptSection } from '../src/core/modes/subscribe/prompt';
import { buildPrompt, buildScoutPrompt } from '../src/core/prompt';
import { historyText } from '../src/core/render';
import type { MemberConfig, ChatMessage } from '../src/core/types';

const member: MemberConfig = { id: 'm1', name: '甲', adapter: 'fake', persona: 'p', color: '#000' };
const members: MemberConfig[] = [
  member,
  { id: 'm2', name: '乙', adapter: 'fake', persona: 'p', color: '#000' },
];

describe('prompt 双语', () => {
  it('baton 段:zh 教 <接棒>(逐字节现状),en 教 <pass>', () => {
    const zh = buildBatonPromptSection(member, members, 'chain', 'zh');
    expect(zh).toContain('**<接棒>@名字**');
    expect(zh).toContain('# 接棒规则 (重要)');
    const en = buildBatonPromptSection(member, members, 'chain', 'en');
    expect(en).toContain('**<pass>@name**');
    expect(en).toContain('# Baton Rules (Important)');
    expect(en).not.toContain('<接棒>');
  });
  it('缺省 lang=zh:与现状一致(既有调用兼容)', () => {
    expect(buildBatonPromptSection(member, members, 'chain')).toContain('<接棒>');
  });
  it('subscribe 段:en 禁 <pass> 指令 + <skip> 出口', () => {
    const en = buildSubscribePromptSection(member, members, { mustRespond: true }, 'en');
    expect(en).toContain('<dm>');
    expect(en).toContain('<skip>');
    expect(en).not.toContain('<跳过>');
  });
  it('historyText:en 标签', () => {
    const msgs: ChatMessage[] = [
      { id: '1', roomId: 'r', from: 'm2', fromName: '乙', text: 'hi', ts: 0 },
    ];
    expect(historyText(msgs, 10, 'm1', '甲', 'en')).toContain('(public to all)');
    expect(historyText(msgs, 10, 'm1', '甲', 'zh')).toContain('(全员公聊)：');
  });
  it('buildScoutPrompt en', () => {
    expect(buildScoutPrompt('/x', 'tree', 'en')).toContain('project scout');
  });
});
```

(buildPrompt 的 en 全量断言在本测试追加一个最小用例:zh 房间 + lang:'en' → 输出含 '# Your Role' 且不含 '# 你的角色'。)

- [ ] **Step 4: 跑全量测试**

Run: `cd server && npm test && npm run typecheck`
Expected: 全绿。若 baton.test/context-mode.test/summary-compact 的 prompt 断言失败 → 检查 zh 拷贝是否逐字节(最常见坑:全角冒号/括号、`\n` 位置)。

- [ ] **Step 5: Commit**

```bash
git add server/src/core/i18n/promptTexts.ts server/src/core/render.ts server/src/core/prompt.ts server/src/core/modes/baton/prompt.ts server/src/core/modes/subscribe/prompt.ts server/src/core/orchestrator.ts server/src/core/modes/subscribe/engine.ts server/tests/prompt-i18n.test.ts
git commit -m "feat(i18n): prompt 段落词典双语化(组装逻辑不动;zh 逐字节;en 标签经真源参数注入)"
```

### Task 9: summaryOps/admin/direct prompt 双语 + en 编排链冒烟

**Files:**
- Modify: `server/src/core/summaryOps.ts`(buildPrivateDigestPrompt 加 lang;:210 `'密信'` fallback、:214-234 各段)
- Modify: `server/src/core/admin.ts`(:163-192 摘要 prompt 段 + 3 个 Markdown 标题;scout prompt 调 buildScoutPrompt 传 this.lang)
- Modify: `server/src/core/direct.ts`(:236-239 delta prompt、:411-435 buildDirectFullPrompt——均加 lang 尾参,内部走词典)
- Modify: `server/src/core/i18n/promptTexts.ts`(追加 direct/summary/admin 段 key)
- Test: `server/tests/prompt-i18n.test.ts` 追加;summary-compact/direct-chat 既有断言不动

**Interfaces:**
- Produces: `buildPrivateDigestPrompt(member, msgs, prev, lang='zh')`;`buildDirectFullPrompt` 私有方法内部用 `this.lang`;admin 摘要 prompt 经 `pt(this.lang, 'a.*')`

- [ ] **Step 1: promptTexts 追加段落 key(zh 拷贝自源文件行锚点,en 翻译)**

key 清单(en 全文关键段落):

- `'dp.task'` en: `[Task Guidance]\nFrom a first-person perspective, sort out and summarize all your private DM exchanges in this room.`
- `'dp.prevLabel'` en: `[Your previous private digest (roll-update and merge on this basis)]`
- `'dp.newRecordsLabel'` en: `[New private records since your last digest (${p?.n} total)]`
- `'dp.recordsLabel'` en: `[Your private DM records in this room (${p?.n} total)]`
- `'dp.outputReq'` en: `Directly output your first-person private memoir, concise and within 500 words, without any opening pleasantries or filler.`
- `'dp.whisper'` en: `'DM'`(zh '密信',:210 fallback 标签)
- `'a.sysrole'` en: `You are the [Admin] of this multi-role discussion. Based on the conversation, produce a clearly structured Markdown [Discussion Summary] with explicit positions.`
- `'a.topicLabel'` en: `[Discussion Topic]`
- `'a.prevLabel'` en: `[Existing summary (roll-merge and update on this basis, within 800 words)]`
- `'a.newMsgsLabel'` en: `[New public speeches since last summary (${p?.n} total)]`
- `'a.recordsLabel'` en: `[Conversation record (${p?.n} total)]`
- `'a.outputReq'` 三标题(en):`### 1. Core Topic & Background` / `Briefly summarize the core question and background.` / `### 2. Positions & Disagreements` / `Sort out each speaker's stance, key arguments, and points of contention.` / `### 3. Consensus & Next Focus` / `Summarize the consensus reached and the next focus to advance.` / 结尾行 en: `Output the Markdown above directly, within 800 words, no opening pleasantries.`
- direct 段:`'dv.greetingNamed'` en: `You are now in an exclusive one-on-one private chat with **[${p?.name}]**.`;`'dv.greetingAnon'` en: `You are now in an exclusive one-on-one private chat with the user.`;`'dv.personaIntro'` en: `Your persona and stance:\n${p?.persona}`;`'dv.userPersonaIntro'` en: `The other side's (user's) identity and background:\n${p?.persona}`;`'dv.replyGuide'` en: `Reply to the other side naturally and sincerely, fully in character.`;`'dv.prevSummaryLabel'` en: `[Earlier conversation summary]`;`'dv.historyLabel'` en: `Here is your previous conversation:`;`'dv.replyPrompt'` en: `Now reply to the other side:`;`'dv.deltaIntro'` en: `Here are the new messages from the user:`;`'dv.keepPersona'` en: `You are **[${p?.name}]**. Always keep your **established persona and core stance**.`

zh 侧全部逐字节拷贝自 direct.ts:236-239/:411-435、summaryOps.ts:210-234、admin.ts:163-192。

- [ ] **Step 2: 三个消费文件接 lang**

1. `summaryOps.ts`:`buildPrivateDigestPrompt(member, privateMessages, prevDigest, lang: Lang = 'zh')`,format 行 `[${m.fromName} -> ${(m.audience ?? []).join(',') || pt(lang, 'dp.whisper')}]: ${m.text}`;调用点 room.ts 私聊纪要生成处(约 :410 executePrivateDigest 传参链)传 `this.lang`。
2. `admin.ts`:`doGenerateSummary` 内 promptParts 全部走 `pt(this.lang, 'a.*', {...})`;scout 调 `buildScoutPrompt(root, tree, this.lang)`。
3. `direct.ts`:`buildDirectFullPrompt` 内段落走 `pt(this.lang, 'dv.*')`;:236-239 增量 prompt 同。

- [ ] **Step 3: 测试追加**

prompt-i18n.test.ts 追加:

```ts
import { buildPrivateDigestPrompt } from '../src/core/summaryOps';
it('私聊纪要 prompt 双语', () => {
  const msgs = [{ id: '1', roomId: 'r', from: 'm1', fromName: '甲', text: 'x', ts: 0, audience: ['m2'] }] as any[];
  expect(buildPrivateDigestPrompt(member, msgs, null, 'zh')).toContain('私聊密信往来');
  expect(buildPrivateDigestPrompt(member, msgs, null, 'en')).toContain('private DM exchanges');
});
```

并在 orchestrator.test.ts 的 en 场景(Step 4 of Task 6)补一条:`expect(h2 fake.requests[0]?.prompt ?? '')`——en harness 下首个请求 prompt 含 '# Your Role' 且不含 '# 你的角色'(需要把 fake.requests 暴露出来;若 en harness 是手搓 deps,直接断言其 fake.requests[0].prompt)。

- [ ] **Step 4: 全量测试**

Run: `cd server && npm test && npm run typecheck`
Expected: 全绿。

- [ ] **Step 5: Commit**

```bash
git add server/src/core/summaryOps.ts server/src/core/admin.ts server/src/core/direct.ts server/src/core/i18n/promptTexts.ts server/tests/prompt-i18n.test.ts server/tests/orchestrator.test.ts
git commit -m "feat(i18n): 摘要/纪要/私聊/侦察 prompt 双语化,B 类收尾"
```

---

## Slice 5:前端 i18n 基建

### Task 10: vue-i18n 装配 + locale 流转 + 切换 UI

**Files:**
- Modify: `web/package.json`(dependencies 加 `"vue-i18n": "^9.14.0"`)
- Create: `web/src/i18n/index.ts`、`web/src/i18n/zh.ts`、`web/src/i18n/en.ts`
- Modify: `web/src/main.ts`、`web/index.html`(title 占位)、`web/src/components/layout/Sidebar.vue`(切换控件)
- Modify: `web/src/services/api.ts`(getSettings/setLang 两方法)

**Interfaces:**
- Produces:
  - `export function initLang(): Promise<void>`(main.ts 调)
  - `export function setLang(lang: 'zh' | 'en'): void`
  - `export function currentLang(): 'zh' | 'en'`
  - `export const t: (key: string, params?: Record<string, unknown>) => string`(全局翻译函数,非组件内可直接 import)
  - api:`api.settings(): Promise<{ lang: 'zh' | 'en' }>`、`api.setLanguage(lang): Promise<{ lang: string }>`

- [ ] **Step 1: 装依赖**

```bash
cd web && npm install vue-i18n@^9.14.0
```

- [ ] **Step 2: zh.ts/en.ts 词典骨架(域结构定稿,文案分批填——本任务只放 app.title + sidebar 域作种子)**

```ts
// web/src/i18n/zh.ts
export const zh = {
  app: { title: 'AI 聊天室' },
  sidebar: {
    rooms: '房间', chars: '角色', newRoom: '＋ 新房间',
    import: '导入', noRooms: '还没有房间', noChars: '还没有角色',
    deleteRoomTitle: '删除房间',
    deleteRoomBody: '删除房间「{name}」?聊天历史文件将保留(重启后不再复活)。',
    delete: '删除',
    importFailedTitle: '导入失败',
    importFailedBody: '文件「{file}」导入失败: {reason}',
  },
  common: { cancel: '取消', confirm: '确认', close: '关闭', save: '保存' },
} as const;
```

```ts
// web/src/i18n/en.ts
import type { zh } from './zh';
export const en: Record<keyof typeof zh, any> = {
  app: { title: 'AI Chatroom' },
  sidebar: {
    rooms: 'Rooms', chars: 'Characters', newRoom: '＋ New Room',
    import: 'Import', noRooms: 'No rooms yet', noChars: 'No characters yet',
    deleteRoomTitle: 'Delete Room',
    deleteRoomBody: 'Delete room "{name}"? Chat history files will be kept (rooms will not revive after restart).',
    delete: 'Delete',
    importFailedTitle: 'Import Failed',
    importFailedBody: 'File "{file}" failed to import: {reason}',
  },
  common: { cancel: 'Cancel', confirm: 'OK', close: 'Close', save: 'Save' },
};
```

(后续任务按域追加 key;en 用 `Record<keyof typeof zh, any>` 松型——嵌套 shape 由 vue-i18n schema 校验 + missingWarn 兜底,**不**做深层递归类型锁定,避免 400 条规模下的类型噪音。)

- [ ] **Step 3: i18n/index.ts**

```ts
// locale 流转:后端 settings 为权威(GET 校正),localStorage 为首屏缓存。
// setLang 即时生效:vue-i18n locale + <html lang> + <title> + PUT 后端。
import { createI18n } from 'vue-i18n';
import { zh } from './zh';
import { en } from './en';

export type AppLang = 'zh' | 'en';

const i18n = createI18n({
  legacy: false,
  locale: 'zh',
  fallbackLocale: 'zh',
  missingWarn: true,
  messages: { zh, en },
});

function applyLocaleSideEffects(lang: AppLang) {
  document.documentElement.lang = lang === 'zh' ? 'zh-CN' : 'en';
  document.title = i18n.global.t('app.title') as string;
}

/** 启动初始化:localStorage 先行(防闪屏),后端 GET 校正(权威)。 */
export async function initLang(): Promise<void> {
  let cached: AppLang | null = null;
  try {
    const v = localStorage.getItem('ai-chatroom:lang');
    if (v === 'zh' || v === 'en') cached = v;
  } catch {}
  let lang: AppLang = cached ?? (navigator.language?.toLowerCase().startsWith('en') ? 'en' : 'zh');
  i18n.global.locale.value = lang;
  applyLocaleSideEffects(lang);
  try {
    const r = await fetch('/api/settings');
    if (r.ok) {
      const j = await r.json();
      if (j.lang === 'zh' || j.lang === 'en') lang = j.lang;
    }
  } catch {}
  i18n.global.locale.value = lang;
  applyLocaleSideEffects(lang);
}

export function setLang(lang: AppLang): void {
  i18n.global.locale.value = lang;
  applyLocaleSideEffects(lang);
  try { localStorage.setItem('ai-chatroom:lang', lang); } catch {}
  void fetch('/api/settings/language', {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ lang }),
  });
}

export function currentLang(): AppLang {
  return i18n.global.locale.value as AppLang;
}

/** 全局 t(非组件上下文用:api.ts/exportMarkdown.ts 等) */
export const t = i18n.global.t;

export default i18n;
```

- [ ] **Step 4: main.ts + api.ts**

```ts
// main.ts
import { createApp } from 'vue';
import App from './App.vue';
import i18n, { initLang } from './i18n';
import './style.css';

createApp(App).use(i18n).mount('#app');
void initLang();
```

api.ts 的 req 泛型函数后追加:

```ts
  settings: () => req<{ lang: string }>('/api/settings'),
  setLanguage: (lang: 'zh' | 'en') =>
    req<{ lang: string }>('/api/settings/language', { method: 'PUT', body: JSON.stringify({ lang }) }),
```

(i18n/index.ts 里直接用 fetch 而非 api.ts——避免循环依赖;api.ts 两方法供其他组件用。)

- [ ] **Step 5: Sidebar 切换控件**

`Sidebar.vue` template 侧栏底部(footer 区,挨着既有 tab/操作区;样式类沿用 `btn btn-ghost` 既有按钮体系,实现时按实际布局微调):

```vue
<script setup>
import { currentLang, setLang } from '@/i18n';
</script>
<template>
  <!-- ...既有内容... -->
  <div class="lang-switch">
    <button type="button" class="btn btn-ghost lang-btn" :class="{ active: currentLang() === 'zh' }" @click="setLang('zh')">中</button>
    <button type="button" class="btn btn-ghost lang-btn" :class="{ active: currentLang() === 'en' }" @click="setLang('en')">EN</button>
  </div>
</template>
<style scoped>
.lang-switch { display: flex; gap: 4px; }
.lang-btn.active { background: var(--accent, #5b6aff); color: #fff; }
</ scoped>
```

(注意:`currentLang()` 在 template 中调用不是响应式依赖——**改用** computed:`const lang = computed(() => currentLang())` 并以 `lang === 'zh'` 判定。locale 切换触发组件重渲染由 vue-i18n reactive locale 保证。)

- [ ] **Step 6: typecheck + 手工冒烟**

Run: `npm run typecheck`(根目录)+ `npm run dev` 起两端。
手工:① 初始中文;② 点 EN → 标题变 "AI Chatroom"(浏览器 tab)、`<html lang="en">`、刷新保持英文;③ 点中回中文;④ `data/settings.json` 内容随切换变化;⑤ en 状态下发一条消息 → 系统提示为英文。

- [ ] **Step 7: Commit**

```bash
git add web/package.json web/package-lock.json web/src/i18n web/src/main.ts web/index.html web/src/services/api.ts web/src/components/layout/Sidebar.vue
git commit -m "feat(i18n): 前端 vue-i18n 基建(locale 流转+切换控件+title/lang 属性)"
```

---

## Slice 6:前端文案抽取(~400 条,按域分 4 个任务)

> **抽取通用纪律(每个任务内适用):**
> 1. template 静态中文 → `{{ t('domain.key') }}`;`:placeholder="..."`/`:title="..."` → `:placeholder="t('domain.key')"`。
> 2. script 内中文 → import `{ t } from '@/i18n'` 后 `t('domain.key', { name })`;`|| '中文兜底'` → `|| t('domain.key')`。
> 3. 三目内嵌中文拆两个 key;状态映射字典改 `{ idle: 'status.idle', ... }` + `t(map[x])`。
> 4. useDialog 调用点传 `t()` 渲染结果(接口不变)。
> 5. 组件 `<script setup>` 加 `const { t } = useI18n()`(组件内)或顶层 import(非组件)。
> 6. 每改完一组组件:`npm run typecheck` + 浏览器走查该组件中英两态。
> 7. **词典 key 命名**:`组件域.语义名`(camelCase),中英同 key;en 翻译风格与 Task 8/10 一致(简洁、祈使句、占位 `{name}`)。
> 8. 注释里的中文**不动**(非用户可见)。

### Task 11: layout + ui 域(Sidebar/RoomTabBar/SidebarCard/Modal/DialogHost/App)

**Files:**
- Modify: `web/src/components/layout/Sidebar.vue`、`web/src/components/layout/RoomTabBar.vue`、`web/src/components/ui/SidebarCard.vue`、`web/src/components/ui/Modal.vue`、`web/src/components/ui/DialogHost.vue`、`web/src/App.vue`
- Modify: `web/src/i18n/zh.ts`、`web/src/i18n/en.ts`(追加 sidebar/tabbar/card/dialog/app 域)
- Test: 手工走查 + typecheck

**Interfaces:** Consumes Task 10 的 `t/useI18n`。

- [ ] **Step 1: 逐文件抽取**

各文件中文点位(普查锚点):
- `Sidebar.vue`(~28 行):标题「AI 聊天室」、tab「房间/角色」、「＋ 新房间」「导入」「还没有房间/角色」、删除房间 dialog 三件套(Task 10 种子已含,补全实际差异)、导出/编辑 hover 文案。
- `RoomTabBar.vue`:tab 关闭 title、溢出提示等全部中文。
- `SidebarCard.vue`(~20):默认 prop 值中文(`导出`/`编辑` 等)——**默认值改为 key**,渲染点 `t()`;prop 类型改 string key 或保留中文缺省改调用方传 key(定稿:prop 缺省改 key `'card.edit'` 类,组件内渲染 `t(props.label)`)。
- `Modal.vue`/`DialogHost.vue`:确认/取消按钮缺省文案走词典。
- `App.vue`:如有顶层文案。

- [ ] **Step 2: 词典追加 + typecheck + 走查**

每个 key zh 从组件原样搬,en 翻译。Run: `npm run typecheck`;浏览器切中英走查侧栏/卡片/弹窗三层。

- [ ] **Step 3: Commit**

```bash
git add web/src/components/layout web/src/components/ui web/src/i18n
git commit -m "feat(i18n): layout/ui 域文案抽取(sidebar/tabbar/card/modal/dialog)"
```

### Task 12: chat 核心域(RoomView/MemberBar/ChatFlow/MessageBubble/Composer/ChatInspector/TraceDetail/UserPersonaSelector/DirectChatView)

**Files:**
- Modify: `web/src/components/chat/` 下 9 个文件
- Modify: `web/src/i18n/zh.ts`、`en.ts`(chat/member/composer/inspector/trace/direct 域)
- Test: 手工走查 + typecheck

**特殊点(必须按此处理):**
1. **MemberBar/RoomView 状态映射**(`待命/思考中/输出中/出错`、模式名、上下文模式名):改 key 映射表 + `t()`。
2. **MessageBubble `batonBadge`/`privateAction` label**(265-303 行):label 改 `t('chat.baton.pass', { name })` 等;**正则匹配部分本任务不改**(Task 15 收口)——本任务只改 label 文案与 `cleanText` 之外的展示文本。
3. **MessageBubble 操作菜单/dialog 文案**(重新生成/清除此后 N 条/复制等)。
4. **Composer**:placeholder、编辑模式徽章、发送/停止/更新三态、@候选提示「↑↓ 选择 · Tab/Enter 确认」、私聊输入提示。
5. **ChatInspector** tab 名(摘要/统计/日志/管理)。
6. **TraceDetail** 面板标签。

- [ ] **Step 1-3: 抽取 → typecheck → 走查 → Commit**(同 Task 11 模式;commit message:`feat(i18n): chat 核心域文案抽取(气泡/编排条/输入框/inspector)`)

### Task 13: modals 域(AddMemberPanel/CharacterModal/NewRoomModal/RoomForm/CharacterForm)

**Files:**
- Modify: `web/src/components/modals/` 下 5 个文件
- Modify: `web/src/i18n/zh.ts`、`en.ts`(form.room/form.char/form.member 域)

**特殊点:** RoomForm(~34 行,模式选择「接棒模式 (默认)」「订阅模式」、上下文模式、创建后不可改提示、字数提示);CharacterForm(~19 行);AddMemberPanel(~25 行,含「已选 N」插值);校验 dialog 文案。

- [ ] **Step 1-3: 抽取 → typecheck → 走查 → Commit**(`feat(i18n): modals 表单域文案抽取`)

### Task 14: inspector 面板域(Summary/Stats/Logs/RoomManage/DirectManage)+ utils/api 基础

**Files:**
- Modify: `web/src/components/chat/inspector/` 下 5 个文件
- Modify: `web/src/utils/avatar.ts`(色板 8 label → key,调用点 `t()`)、`web/src/services/api.ts`(错误 `请求失败(${status})` → `t('api.requestFailed', { status })`——api.ts import `{ t } from '@/i18n'`,运行时取值)
- Modify: `web/src/i18n/zh.ts`、`en.ts`(stats/summary/logs/manage/palette/api 域)

**特殊点:** InspectorSummary(~45 行:摘要文件列表、生成中/失败反馈);InspectorStats(~41 行:统计字段名、`获取统计数据失败` 兜底、`toLocaleString()` 传 locale 参数 `(num||0).toLocaleString(currentLang() === 'zh' ? 'zh-CN' : 'en-US')`);InspectorLogs(~29 行)。

- [ ] **Step 1-3: 抽取 → typecheck → 走查 → Commit**(`feat(i18n): inspector 面板域+avatar/api 层文案抽取`)

---

## Slice 7:导出与收尾

### Task 15: exportMarkdown/markdown.ts 双语 + 前端协议真源收口

**Files:**
- Modify: `web/vite.config.ts`(:9 注释扩例外)
- Modify: `web/src/utils/exportMarkdown.ts`(~50 条 + stripControlTags/resolveBatonBadge/isSchedulerMessage 正则改引真源)
- Modify: `web/src/utils/markdown.ts`(:24 复制按钮 `t()` 注入)
- Modify: `web/src/components/chat/MessageBubble.vue`(:17/:291/:294 正则改引真源)
- Modify: `web/src/i18n/zh.ts`、`en.ts`(export 域)

**Interfaces:**
- Consumes: Task 1 真源常量(web 经 `@server/protocolKeywords` runtime import)

- [ ] **Step 1: vite.config 注释更新**

```ts
    alias: {
      // 类型共享:web 经 type-only import 引用 server 源码类型;
      // 唯一运行时例外:@server/protocolKeywords(根级零依赖纯常量叶子,协议标签真源)
      '@server': fileURLToPath(new URL('../server/src', import.meta.url)),
      '@': fileURLToPath(new URL('./src', import.meta.url)),
    },
```

- [ ] **Step 2: 前端协议消费收口**

`MessageBubble.vue`:

```ts
import { BATON_STRIP, BATON_LINE, BATON_END_WORDS, USER_NAME_ALIASES } from '@server/protocolKeywords';
// :17 cleanText
return raw.replace(BATON_STRIP, '').trimEnd();
// :291 batonBadge 兜底解析
const m = raw.match(BATON_LINE);
// :294
if (BATON_END_WORDS.test(target)) ...
// :297
if (USER_NAME_ALIASES.includes(target.toLowerCase())) ...
```

`exportMarkdown.ts` 同法:`stripControlTags` 用 `BATON_STRIP` + 新增需求——`<私聊>/<同意>/<拒绝>/<想法>` 剥除需要 DM_STRIP/HANDSHAKE_STRIP,**但当前 stripControlTags 只删 `<私聊>` 行**(不删【私聊】,也不删握手行原文?核对 :45-49:删接棒行/私聊行/同意行/拒绝行/想法行五类)——替换为 `BATON_STRIP`、`DM_STRIP`、`HANDSHAKE_STRIP` 三正则链;`resolveBatonBadge` 用 `BATON_LINE/BATON_END_WORDS/USER_NAME_ALIASES`;`:94 isSchedulerMessage` 的中文系统消息正则 `/把接棒交给|指定.*接棒|宣布讨论结束|把话题交还给了你/` **不改**——它匹配的是**历史系统消息文本**(落库已是烘焙后文本,中英混合);扩展为并集 `/把接棒交给|指定.*接棒|宣布讨论结束|把话题交还给了你|passed the baton|declared the discussion closed|handed the topic back/`。

- [ ] **Step 3: exportMarkdown 文案双语(~50 条)**

export 域 key 结构(zh 原样/en 翻译;节选代表性 key,en 风格据此):

```ts
export: {
  docTitle: '对话纪要：{title}' / 'Chat Transcript: {title}',
  sessionType: '会话类型' / 'Session type',
  sessionRoom: '群聊房间' / 'Group room',
  sessionDirect: '1v1 专属私聊' / '1v1 private chat',
  members: '参与成员' / 'Participants',
  exportTime: '导出时间' / 'Exported at',
  recordCount: '记录总数：{n} 条' / 'Messages: {n}',
  sysNotice: '系统通知' / 'System notice',
  userFallback: '用户' / 'User',
  unknownMember: '未知成员' / 'Unknown member',
  privateVisible: '仅 {list} 可见' / 'Visible only to {list}',
  roundInfo: ' (第 {n} 轮)' / ' (round {n})',
  batonGive: '🎯 接棒给 @{name}' / '🎯 baton to @{name}',
  batonEnd: '🏁 宣布讨论结束' / '🏁 declared the discussion closed',
  batonToUser: '🤝 话题交还给用户' / '🤝 topic handed back to the user',
  fileChatPrefix: '[聊天记录]' / '[Chat transcript]',
  fileSummaryPrefix: '[讨论摘要]' / '[Discussion summary]',
  roomFallbackTitle: '房间聊天记录' / 'Room chat transcript',
  directFallbackTitle: '专属私聊记录' / 'Private chat transcript',
  summaryTitle: '讨论摘要：{title}' / 'Discussion Summary: {title}',
  snapshotId: '快照标识' / 'Snapshot ID',
  genTime: '生成时间' / 'Generated at',
  triggerAuto: '自动滚动提炼' / 'auto rolling',
  triggerManual: '手动即时生成' / 'manual',
  coverCount: '覆盖对话量：约 {n} 条' / 'Covers ~{n} messages',
  outlineSection: '一、公聊讨论大纲' / '1. Public discussion outline',
  // …(摘要导出其余字段同风格补全;实现时对照 exportMarkdown.ts :100-260 全量)
}
```

`buildChatHistoryMarkdown/buildSummaryMarkdown` 内部改 `t('export.xxx', {...})`(import `{ t } from '@/i18n'`;`formatFullTime` 保持 `YYYY-MM-DD HH:mm:ss` 手写格式,两语言通用,不引入 Intl)。

- [ ] **Step 4: markdown.ts 复制按钮**

`:24` 模板串改:

```ts
import { t } from '@/i18n';
// 渲染器初始化处(或 button 拼接处):
`<button class="copy-code-btn" type="button" data-copied="${t('common.copied')}">${t('common.copy')}</button>`
```

注意:markdown 渲染有 HTML 缓存——切语言后旧缓存不重译(预期行为,spec §6.3)。`common.copy: '复制'/'Copy'`、`common.copied: '已复制'/'Copied'` 入 common 域;MessageBubble.vue:323 `'已复制!'` 同步改 `t('common.copied')`。

- [ ] **Step 5: typecheck + 走查**

Run: `npm run typecheck`;浏览器:en 下导出聊天记录/摘要 → 下载的 .md 打开检查标题字段英文;中英消息混合的房间导出正常;气泡接棒徽章对英文标签消息(`<pass>@Bob`)正确显示。

- [ ] **Step 6: Commit**

```bash
git add web/vite.config.ts web/src/utils web/src/components/chat/MessageBubble.vue web/src/i18n
git commit -m "feat(i18n): 导出文档/markdown 复制按钮双语 + 前端协议解析收口至 @server 真源"
```

### Task 16: 端到端验收 + CLAUDE.md 更新

**Files:**
- Modify: `CLAUDE.md`(架构树加 protocolKeywords.ts 与 core/i18n/;@server 例外一句)

- [ ] **Step 1: 全量自动验证**

```bash
npm test && npm run typecheck && npm run build
```

Expected: server 全部测试绿、两包 typecheck 绿、web 生产构建成功。

- [ ] **Step 2: 手工验收清单(spec §7,逐项过并记录)**

1. zh 模式全流程与改造前无差异(建房/加成员/接棒链/轮流/订阅心跳/私聊握手/摘要/导出)
2. 切 en → 界面全英;系统消息(加人/离人/接棒传递/停止思考/轮流开始结束)英文
3. en 模式发起讨论 → 发给 AI 的 prompt 英文(trace 面板查 prompt 全文)、AI 写英文标签(`<pass>@名`)、接棒链正常传递(trace 日志验证解析)
4. en 模式私聊:AI 写 `<dm>@名`/`<agree>` → 拆分气泡与握手状态正确(Inspector 日志 + 气泡 🔒 徽章)
5. 切回 zh → 新系统消息回中文;中英混合历史正常显示与导出
6. en 导出 Markdown 标题/字段英文;含中文历史消息正文原样保留
7. 刷新页面语言保持(后端 settings.json 权威;清 localStorage 后仍按后端)
8. 旧数据兼容:改造前的中文房间/角色/历史消息在 en 模式正常显示、@点名、重 roll、编辑截断

- [ ] **Step 3: CLAUDE.md 架构节更新**

```
server/src/
├── paths.ts              REPO_ROOT 路径锚定(零依赖叶子)
├── protocolKeywords.ts   协议关键字真源(接棒/私聊/握手/沉默双语标签+词表;
│                         根级零依赖叶子,core/store/web(@server 例外)三方共用;
│                         匹配=中英并集,prompt 教学=按语言单侧)
...
├── core/
│   ├── i18n/             语言层:lang.ts(Lang/LangGetter)+ messages.ts(A 类词典+t)
│   │                     + promptTexts.ts(B 类词典+pt);getLang 构造注入,缺省 zh
```

并在「前端」节补:`web/src/i18n/`(vue-i18n;后端 settings 权威+localStorage 首屏);@server type-only 约定的 protocolKeywords 例外已在 vite.config 注释。

- [ ] **Step 4: Commit**

```bash
git add CLAUDE.md
git commit -m "docs(i18n): CLAUDE.md 架构节补 protocolKeywords 与 core/i18n"
```

---

## Self-Review 记录(计划自审,已执行)

1. **Spec 覆盖**:spec §3(架构/流转)→ Task 4/5;§4(协议)→ Task 1/2/3;§5.1(A 类)→ Task 4/6/7;§5.2(B 类)→ Task 8/9;§6(前端)→ Task 10-14;§7(测试)→ 各任务内嵌 + Task 16;§8 切片对应;§9 风险(标签误触发→测试用例 `<pass>recommend`;A/B 混杂→Task 6/7 表格逐行锚点;前端双份解析→Task 15)✓
2. **占位符扫描**:promptTexts 的 `'sb.mustRespond'` en 侧发现一个 `<沉默>` 残留——已在 Task 8 Step 1 注明改为 `silentTag2` 参数;export 域词典"其余字段同风格补全"指向 exportMarkdown.ts:100-260 的行锚点清单(源码即完整清单,zh 原样搬,非 TBD)✓
3. **类型一致性**:`t(lang, key, params)`(后端)与 `t(key, params)`(前端 vue-i18n)不同签名已用 `pt` 区分后端 prompt 词典;`LangGetter` 全链一致;`makeRoomConfig(input, lang)` 双参在 Task 7 定义、routes 调用点同步 ✓
