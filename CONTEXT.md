# CONTEXT — 领域词汇表(AI 聊天室 v2)

> 本文件是 glossary,只记领域概念与术语,不记实现细节。术语冲突时以本表为准。
> 由 domain-modeling skill 维护;变更应随决策即时落盘。
> 与 docs/design-grill-2026-09-03.md 的关系:该文档管 2026-09 前的聊天室功能决策;任务模式及权限真实化(2026-09-24 起)以本表 + docs/adr/ 为准。

## 核心概念

### 房间(Room)
多参与者(agent 成员 + 用户)共享一条消息流的会话容器,底层引擎统一(编排/持久化/WS/trace 无分叉)。两个 kind(表皮分类,见 ADR-0001):

- **聊天房(chat room)** — `kind: 'chat'`。多 agent 围绕话题讨论/辩论/角色扮演,消息互相可见,编排接棒/轮流/订阅驱动发言。
- **任务房(task room)** — `kind: 'task'`。用户向 agent 派发工作(当前:编程任务)的会话;侧栏"任务" tab 一等公民呈现。约束:projectPath 必填(工作区锚定,**非文件系统硬边界**——Read/Write 不按路径限制)、mode 恒 baton(单成员退化为问答)、默认 full/stateful、无 Scout。成员 1 起步可加(多 agent 协作:A 写码 + B review)。
  - **多成员协作语义**(2026-09-24 审计定案):复用既有手动 @ 点名驱动(成员干完即停,停止后用户 @ 下一位)——零新机制,既有 @ 待命/停止语义原生承载。无自动链,监督权在用户。
  - **导入降级**(2026-09-24 审计定案):导入时 projectPath 本机不存在 → task 房**降级为 chat 房**导入 + 系统消息警示(保"导入永不失败"原则,不产无项目僵尸房)。

### 角色(Character)
全局一等实体(角色库),独立于房间存在。人设/立绘/适配器的定义源。拉入房间时复制快照为成员。

### 成员(Member)
角色快照在房间内的实例。同名可多实例(最小空闲后缀去重)。

### 适配器(Adapter)
调用一种 agent CLI 的方式(`agents.yaml` 注册)。适配器 ≠ 角色 ≠ 成员(三概念分离)。

### 私聊(Direct / 1v1)
角色本位的闲聊会话,**锚定 characterId**,与房间物理隔离(独立历史/会话/摘要)。不是任务载体——任务刚需(项目目录/写权限/多实例/重启续跑)在私聊层缺失,任务统一走任务房。

### 编排(Orchestration)
驱动 agent 发言的状态机(idle/baton/roundrobin/subscribe)。聊天房的灵魂;任务房中退化为"一问一答"。

## 任务相关(2026-09-24 定案)

### 任务(Task)
用户派给任务房的一件工作。任务 = 任务房 + 派单内容。不设独立 Task 实体、不另写引擎——kind:'task' 的房间即任务,底层与聊天房共用全部引擎(见 ADR-0001)。

任务房配置束:权限 `full` 默认(可选 readonly;依赖权限真实化先行)、上下文 `stateful` 默认(与直接用 CLI 同构:增量 prompt + resume,历史本体在 CLI session)、mode 恒 baton、成员 1 起步可加、`projectPath` 必填(不落共享临时目录)、无 Scout 预检(任务 agent 自主探索)。

任务 prompt 契约(2026-09-24 定案):任务教学段族替换讨论+接棒教学——工作区段(项目路径 + ADR-0002 真实权限说明 + "工作仅限此项目目录"范围约定 + 自主探索引导,**不注入目录树**,agent 用工具自查实时结构)、行为契约(实际用工具改码勿只描述/汇报做了什么改了哪些文件如何验证/遇阻先问/不限长度)。persona 照常注入(表单提供工程师模板便利填充);砍掉:参与者列表、接棒教学、长度约束。

### 工具权限档位(ToolPermission)
房间级枚举 `readonly | readwrite | full`,成员继承,适配器翻译为各 CLI 硬闸参数(见 ADR-0002)。语义跨 CLI 统一——**工具级闸门,非路径级边界**:
- readonly = 拒写/拒命令/拒网络(全盘;读不限路径)
- readwrite = 读写文件,拒命令/拒网络/拒子代理(写不限于项目内——绝对路径可越界,路径约束靠 prompt 约定)
- full = 不限(含命令、网络、子代理)

### 项目目录(ProjectPath)
任务房绑定的目录。agent 的**工作区锚点**(cwd:相对路径落点 + 项目级 CLAUDE.md/skills 加载源)。**不是文件系统边界**——CLI 工具不按 cwd 限制读写范围。建房后不可变(session resume 依赖 cwd 稳定)。

### 斜杠命令(Slash Command)
输入框 `/` 触发的命令候选层(与 @ 提及同套弹层与解析管线,mentions.ts 单一真源)。两层命令集:
- **内置命令**(硬编码):`/compact`(压缩当前 agent session 上下文,本地动作)、`/model <name>`(切模型,下次发言生效)。不做 /skills 询问(skill 候选列表已覆盖盘点)。
- **skill 命令**(动态):扫描 `~/.claude/skills/` + 项目 `.claude/skills/` 生成候选;选中后组装"使用 X skill 执行:<补充说明>"的 prompt 发给 agent——skill 触发经自然语言(实测可用),非 CLI 命令透传(`/skills` `/mcp` 在 headless -p 模式不存在,实测确认)
- 边界:skill 用不用是模型决策(prompt 强指令,实测即从,非硬保证);未知 `/xxx` 不拦截,当普通文本发出
- 作用域:当前房

### 任务状态(Task Status)
任务的三态:空闲 / 进行中 / 出错。**单成员任务房里 = 成员状态直通映射**(thinking/streaming→进行中;error→出错;其余→空闲),零推导、零持久化、零后端改动;重启归零(与"编排运行态不持久化"边界一致)。无"待验收/完成"态——验收是人的判断,由用户看消息流自行处置(重跑走既有重 roll,继续派活即回进行中)。

### 超时保护(Task Timeout)
单次发言可配时长上限(agents.yaml 全局配置项 + 房间可调,**默认无超时**——用户手动启用才生效)。超时 = 杀进程树 + 落系统消息("超时已停止,可重发继续"),**走 cancelled 轨道**(严禁 error 轨道:其自愈会清 sessionIds 并自动重试,语义相反);**stateful 房超时不清 sessionIds**——CLI 侧历史仍在,重发即 resume 续跑,前功不弃。另:前端气泡展示"最后事件时间",长时无输出提示(纯前端,读现有事件流)。

## 已知边界(任务模式,2026-09-24 记录)

- **无并发上限**:任务房并行无闸,同时派 N 个任务 = N 个 CLI 并发烧钱(单用户自担;加闸 = 编排层全局信号量 + "排队中"第四态,待有真实痛感再做)
- **无断点续跑**:重启杀进程树,进行中任务中断且无提示;中断后用户发"继续"即可(stateful resume 续跑,CLI 侧上下文仍在,实际损失小);自动检测"中断"无可靠机器信号(诚实做法是不做半吊子检测)
- **路径无硬边界**:三档管工具种类不管路径(ADR-0002),任务房越界读写靠 prompt 约定缓解
